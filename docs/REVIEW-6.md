# Adversarial review — round 6

**Date:** 2026-08-19 · **Result:** 72 findings raised across five surfaces. **26 were independently
verified; 23 survived** — 10 severe, 10 moderate, 3 minor. **46 findings from three surfaces were
never assessed**, because those verifiers died mid-run.

## Read this section before the findings

This round ran across four attempts, losing agents to a DNS outage, a session limit and a dropped
connection. What that leaves is **unevenly verified**, and the distinction matters because round 5
demonstrated that unverified findings are unreliable in both directions — its verifiers rejected 4 of
62, and one of them corrected a *round-4* result that was wrong by a factor of 3–4.

| Surface | Raised | Verified | Status |
|---|---|---|---|
| `bench-stats` | 13 | 11 survived of 13 | **Fully verified** |
| `fix-created-holes` | 13 | 12 survived of 13 | **Fully verified** |
| `cleanup-arithmetic` | 13 | — | **Unassessed** — verifier died |
| `cross-doc` | 19 | — | **Unassessed** — verifier died |
| `unattacked-surfaces` | 14 | — | **Unassessed** — verifier died |
| `profiles-design` | — | — | Attack agent died before producing findings |

Only the first two blocks below are safe to act on. The 46 unassessed findings are preserved in the
run transcript and are listed by title at the end; **do not treat them as established.**

## The headline

For the **fourth consecutive round**, the previous round's fix created this round's hole — and this
time it happened *inside a single commit*, with two round-5 fixes breaking each other. `standard`'s
overall CI is published twice, 35 lines apart, at ±6.5% and ±5.7%: the corrected *estimator* was
applied with the *uncorrected* category set. Both fixes are listed as landed in the same pass. They
do not compose.

The second headline is worse and is mine. **Three of the five severe `bench-stats` findings say the
statistical machinery cannot be built as specified** — not that it is mis-tuned, that it is
undefined. `task_score` is continuous, McNemar is a binary test, and **no pass predicate exists
anywhere in 26 documents**. Simulated at the documented weights: 97,838 distinct score values,
P(score = 1.0) = **0.0%**. `throughput_score` — "tasks passed per wall-clock hour", a headline metric
in three documents — is literally uncomputable.

---

## Verified findings

### `signtest-null-false-by-construction` — The precomputation detector's sign test has a null hypothesis that is false by construction, and fires on ~100% of honest runs

**SEVERE** · *verifier lowered severity* · surface: `bench-stats` · docs: `docs/adr/0009-paired-core-and-fresh-probe-seeds.md`, `docs/10-integrity.md`, `docs/07-statistics.md`, `docs/14-roadmap.md`

**Defect.** ADR-0009 and 10-integrity.md specify `precompute_signal = sign_test(family-paired core-vs-probe discordance)` and justify it with "Under honest execution the discordance is symmetric." The test needs one bit per family from core and one from probe. But `deep` runs FOUR core seeds per family against ONE probe seed (1088 core / 163 probe), and `standard` runs TWO against one. No document anywhere says how the k core seeds collapse to the single bit, and no document defines a pass predicate on the continuous `task_score` in the first place. The stated symmetry holds ONLY at k=1. For every k>1 the imbalance is structural, not evidential.

**Failure scenario.** An honest submitter completes a `deep` run. The server collapses the family's 4 core seeds with the most natural rule and compares against the 1 probe seed. Simulation (163 probed families, ICC 0.3, layered-oracle score model): false-accusation rate is 100.0% for the `any` rule at every threshold tested (1e-9, 0.3, 0.5, 0.7, 0.9), 99.9-100% for `all`, 24.9-100% for `mean>=thr`, and 5.9-88.4% for `majority`. Only `first seed` (k=1, discarding 3 of 4 core seeds) is calibrated, at 2.8-4.5%. An independent analytic model with no oracle simulation at all (family pass-prob ~ Beta(1.2,2.0), k core seeds vs 1 probe seed) agrees: E[b]/E[c] = 8.54 and sign-test |z| = 6.7 for `any` at k=4; 0.07 and |z| = 6.4 for `all`; |z| = 3.7 at k=2 for both. |z| = 0.0 only at k=1. Two differently-shaped models, same conclusion.

**Evidence.** Simulation /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/signtest.py; independent analytic cross-check /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/analytic.py. Spec text: docs/adr/0009-paired-core-and-fresh-probe-seeds.md:36-38 ("Count families where core passed and probe failed against the reverse"), docs/10-integrity.md:63-66.

**Resolution.** Three things must be written down before bench-stats is built. (1) A pass predicate on `task_score`, stated once and used everywhere. (2) The core-seed collapse rule -- and the only one that preserves the sign test's null is one core seed against one probe seed, drawn at a pre-registered index, which discards 3/4 of the available core evidence. (3) If that cost is unacceptable, replace the sign test with a test whose null survives unequal k: a paired comparison of the family's mean core score against its probe score (Wilcoxon signed-rank or a cluster bootstrap of the mean difference), which uses all seeds and is symmetric under honest execution by construction. Note that R3-S3's published ~5.2-point sensitivity was necessarily computed at k=1 and does not transfer to any k>1 rule.

**Verifier.** CONFIRMED mathematically and by a third model shape. I derived the exact identity E[b]-E[c] = E[P(B=1|p)] - E[p] for family pass-prob p, k core seeds vs 1 probe seed: the sign test's null holds iff the collapsed core bit is marginally calibrated to a single seed. Every deterministic k>1 collapse breaks it. Logistic random-effects MC over 163 families, 2000 honest runs: any k=4 100%, all k=4 100%, majority k=4 53.7%, mean>=0.5 k=4 51.2%, pick-one 4.2%; at k=2 (standard) 96-98% for all rules but pick-one (3.3%). Spec text verified verbatim: ADR-0009:36-39 and 10-integrity.md:63-66 assert 'honest execution is symmetric' with no collapse rule and no pass predicate. NOT covered by rounds 1-5. Severity lowered to moderate on three reachability grounds the finding does not account for. (1) The failure is LOUD, not silent - a 100% flag rate on honest runs is discovered on the first test run, and no leaderboard number depends on the detector. (2) REVIEW-4.md:29 already names the baseline collapse in-repo: 'Family-aggregate pairing (compare all 4 core seeds against the probe rather than one designated seed...)' - the parenthetical establishes 'one designated seed' (k=1, calibrated) as the default reading. (3) docs/14-roadmap.md:106-107 already schedules 'Estimate baseline core-vs-probe discordance per family, which the sign-test detector needs in order to be calibrated', so a calibration step exists before deployment. It remains a real spec gap because the two normative documents state a symmetry that is only true at k=1 and never say so.

**Verifier correction.** Drop the absolute claim 'No document anywhere says how the k core seeds collapse to the single bit'. REVIEW-4.md:29 does say it ('rather than one designated seed'). The accurate statement is that neither NORMATIVE document (ADR-0009, 10-integrity.md) states the rule, and the symmetry both publish is valid only at k=1. Add the closed-form result, which is stronger than any simulation: under honest execution E[b]-E[c] = E[P(B=1|p)] - E[p], so the sign-test null survives exactly those collapse rules whose output is marginally distributed as a single seed - 'pick a pre-registered index' and randomised selection, nothing else. Also note that REVIEW-4.md:29's own family-aggregate simulation reported 0% detection at 20% precomputation, which is not reproducible under an uncalibrated k=4 sign test (it would fire on 100% of cells including the honest one), so R4-S1's family-aggregate row is itself suspect and should be re-run.

---

### `mcnemar-applied-to-continuous-task-score` — McNemar is specified for model comparison but `task_score` is continuous, no pass predicate exists anywhere, and three published metrics are therefore uncomputable

**SEVERE** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/03-oracle.md`, `docs/12-schemas.md`, `docs/00-overview.md`, `docs/05-hardware-and-calibration.md`, `docs/adr/0009-paired-core-and-fresh-probe-seeds.md`

**Defect.** docs/07-statistics.md:294 "Model comparisons use McNemar on the shared seed set, and state the discordant-pair count." McNemar is a test for paired BINARY outcomes. docs/03-oracle.md:14 defines `task_score = w_b*behavior + w_c*constraint + w_q*quality`, continuous on [0,1] -- behavior is itself a weighted mean of a unit-test fraction and two sub-oracles, constraint a weighted mean of five gates, quality a mixture including a continuous mutation score. A full-repo grep finds no definition of a pass/fail predicate on `task_score` anywhere in 25 documents. Three published metrics depend on that missing predicate: `throughput_score` ("tasks passed per wall-clock hour", 00-overview.md:37; `tasks_passed / wall_clock_hour`, 05-hardware.md:84; `"unit": "units_passed_per_hour"`, 12-schemas.md:188) and `time_to_first_pass_s`. This is orthogonal to R5's `ttfp-undefined-in-repair`, which is about which repair attempt counts, not about what "pass" means on a continuous score.

**Failure scenario.** Simulating the layered oracle at the documented default weights (200k units): `task_score` takes 97,838 distinct values, P(score = 0) = 47.5% (the L0/L1 gate), P(score = 1.0) = 0.0%, and 52.5% of units land strictly between. `throughput_score` therefore reads either 0 units/hour (if "passed" means score = 1.0, which never happens) or an arbitrary number set by an unwritten cut. On the comparison itself, at a true 5-point gap over a 40-family core category with rho = 0.25, the paired cluster bootstrap on the continuous score reaches 41.4% power; McNemar dichotomised reaches 28.2% at the best cut (0.5), 21.7-27.3% at other cuts, 18.7% at "compiled", and 0.0% at "perfect". The design's headline "free 2-4x power" from pairing is real -- measured 2.39x at rho=0, 2.34x at rho=0.25, 1.93x at rho=0.5 -- but ONLY on the continuous outcome with a cluster bootstrap. The estimator the design actually names throws away ~1.5x of it.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/mcnemar.py (distribution, power table) and shop.py part (d) (pairing gain). Grep confirming no pass predicate: no match for a task_score threshold in docs/*.md or docs/adr/*.md.

**Resolution.** Delete McNemar from the design. The correct paired estimator for a continuous bounded outcome is already specified elsewhere in the same document: the cluster bootstrap of the paired mean difference (07-statistics.md:278). Make that the sole model-comparison test, drop "state the discordant-pair count" (undefined for continuous data) in favour of reporting the paired mean difference with its cluster-bootstrap CI, and replace it in 07:294, ADR-0009:9/31, and 10-integrity.md:58. Separately, `throughput_score` and `time_to_first_pass` need either an explicit, pre-registered pass predicate written into 12-schemas.md, or redefinition on the continuous score (e.g. score-mass per hour), because as written they cannot be computed.

**Verifier.** CONFIRMED on the text and on the power claim. 07-statistics.md:294 reads exactly 'Model comparisons use McNemar on the shared seed set, and state the discordant-pair count.' 03-oracle.md:14-17 defines task_score as a continuous [0,1] composite ('Grading produces a vector, not a bit', 03:3). An exhaustive regex sweep of all 34 docs (score>=, 'counts as a pass', 'successful if', threshold-on-score) returns nothing: there is no pass predicate anywhere. Three published metrics depend on it - throughput_score (00:37, 05:84, 12-schemas:188 'units_passed_per_hour'), efficiency_score (00:44), time_to_first_pass (00:46) - and throughput is one of the two numbers the README's whole thesis names, so a headline metric is uncomputable as written. Independent power sim (compile gate + 24-test binomial behavior + 5-gate constraint + beta quality, 40 families x 4 seeds, rho 0.6, 1500 trials): continuous paired cluster bootstrap 45.8% power vs McNemar 26.9% at the best cut, ratio 1.70x (reviewer measured 1.47x - same direction, same order). Not covered by rounds 1-5: REVIEW.md M1 noted the binomial CI on a continuous score and closed it as a conservative bound; nothing addresses McNemar or the missing predicate.

**Verifier correction.** Two fixes and one addition. (1) 'P(score = 1.0) = 0.0%' is an artifact of the reviewer's generative model, not a property of the design - 03-oracle.md permits behavior = constraint = quality = 1.0, so task_score = 1.0 is attainable. Drop the 'throughput_score reads 0 units/hour' branch and rest the claim solely on the predicate being undefined. (2) The scope is wider than the finding states: pass@5 (07:253), 'Full trajectory required for passing units' / 'T1 rejects passing units without them' (10-integrity:165), and 15-profiles' verdict_agreement and 'paired oracle-verdict flips' (15:36, 15:560, 15:126) all consume the same missing predicate. (3) ADD a second, independent defect on the same line: McNemar as specified treats the 160 units of a core category as independent pairs and has no clustering term at all, while 07:278 says paired comparisons must resample families. So 07 specifies two mutually inconsistent model-comparison estimators - the cluster bootstrap of the paired difference (07:240-241, 07:278) and unclustered McNemar (07:294) - and the second would inflate type-I error even if the predicate existed.

---

### `dichotomisation-threshold-shopping` — With no dichotomisation cut specified, an analyst sweeping plausible cuts gets 23.3% type-I error and can move the reported effect size by a median 7.5 points on identical data

**SEVERE** · *verifier lowered severity* · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/12-schemas.md`, `docs/10-integrity.md`, `docs/adr/0009-paired-core-and-fresh-probe-seeds.md`

**Defect.** Because both the McNemar comparison (07:294) and the sign-test detector (ADR-0009:36) require a bit and `task_score` is continuous, whoever implements `bench-stats` chooses a threshold. That choice is unconstrained, unrecorded, and not part of any schema, row key or replay artifact -- so it is invisible to T1 replay, to the public dump, and to independent re-derivation, which docs/README.md calls "the strongest integrity control the project has".

**Failure scenario.** Two IDENTICAL models over a 40-family core category, 4 seeds, 3000 trials. A fixed pre-registered cut gives a correctly-sized test (4.9-6.1% across nine cuts). An analyst who tries all nine and reports the one that reaches significance gets 23.3% type-I error -- 4.7x nominal. On effect size, at a true continuous gap of +5.0pp, the reported gap by cut ranges over a median of 7.50pp within a single dataset (95th percentile 13.12pp, maximum 21.88pp). The design's own claimability bar is that "Two models must differ by roughly 15 points before a core-category difference is claimable" -- an unrecorded analyst choice can manufacture most of that on its own. This also composes with the leaderboard's incentives: the submitter does not choose the cut, but the project does, once, for every published comparison, with no record of the alternatives.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/shop.py parts (a) and (b).

**Resolution.** The cleanest fix is to remove the degree of freedom entirely by adopting the continuous paired cluster bootstrap (see `mcnemar-applied-to-continuous-task-score`), which has no cut to shop. If any dichotomised statistic is retained -- the precomputation detector may need one -- the predicate must be (a) written into docs/12-schemas.md as a versioned constant, (b) included in `suite_version` or the row key so a change is visible, and (c) frozen before the epoch's data exists. Add a reporting rule to 07-statistics.md forbidding post-hoc threshold selection.

**Verifier.** Numbers REPLICATE under an independently written model. 2000 trials, 40 families x 4 seeds, nine cuts: fixed pre-registered cut 0.5 gives 3.4% type-I; sweeping all nine and reporting the best gives 14.5% - 4.3x nominal (reviewer measured 4.7x). Effect-size spread within a single dataset at a true 5.0pp continuous gap: median 7.50pp, p95 13.75pp, max 20.00pp (reviewer 7.50/13.12/21.88). Independent agreement is close. The architectural point is the durable one and is novel: the cut appears in no schema, no row key and no replay artifact, so it is invisible to T1 replay and to the independent re-derivation that README calls 'the strongest integrity control the project has'. Lowered to moderate because the failure scenario as written is contingent on an analyst actually sweeping cuts, and the design has a single analyst who writes bench-stats once - the reachable defect is the unrecorded, unversioned constant, not deliberate shopping, and that is the same root cause already carried by mcnemar-applied-to-continuous-task-score. Reporting it at severe would triple-count one root cause across three severe findings.

**Verifier correction.** Reframe from 'an analyst sweeping plausible cuts' to 'an unrecorded, unversioned constant that moves the published effect size by a median 7.5pp'. The shopping figure is the upper bound on the harm, not the expected case. Keep the resolution as written - the cut belongs in 12-schemas.md as a versioned constant inside suite_version - and note that if the continuous cluster bootstrap replaces McNemar per the companion finding, only the precomputation detector still needs a cut, which shrinks this to one predicate rather than two.

---

### `no-multiplicity-correction-anywhere` — There is no multiple-comparison correction anywhere in the repository, and the radar chart -- the product's headline feature -- carries a 94-99% family-wise error rate

**SEVERE** · *verifier lowered severity* · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/15-profiles-and-divisions.md`, `docs/04-categories.md`, `docs/13-architecture.md`, `docs/adr/0008-core-and-probe-categories.md`

**Defect.** A grep for bonferroni, holm, sidak, multiplicity, family-wise, FWER, false discovery, FDR and per-comparison across all 25 documents returns zero hits. Meanwhile docs/07-statistics.md:293 explicitly licenses the comparisons: "Category comparisons within a model use the paired bootstrap, not overlapping-CI eyeballing", and :294 licenses model-vs-model comparisons, all at alpha 0.05. The leaderboard renders eleven per-category scores with CIs (15-profiles §10.2) and the design's stated purpose for the radar chart is claims of the form "better at traits than at lifetimes" (REVIEW.md S3).

**Failure scenario.** One model's radar chart licenses C(11,2) = 55 within-model category comparisons. At a correctly-sized 5% per-comparison rate the family-wise error rate is 94.05% and the expected number of false orderings is 2.8. At the per-comparison rate actually delivered by the specified estimator (8.5%, measured -- see `percentile-bootstrap-undercovers-at-small-cluster-counts`) the FWER is 99.24% and the expected count is 4.7. So the first radar chart the project publishes will, with near-certainty, contain at least one category ordering that is not real -- and this is the feature REVIEW.md S3 reshaped the entire category design to make supportable. Across the leaderboard it is worse: a 10-model board makes 45 x 11 = 495 category comparisons (24.8 expected false claims); a 20-model board makes 2,090 (104.5 expected false claims). The design has no mechanism that would ever notice.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/boot_fix.py part (C), with the 8.5% per-comparison rate taken from part (B) of the same script.

**Resolution.** Decide and document the inferential family, then correct within it. Concretely: (1) for the within-model radar, either apply Holm-Bonferroni over the 55 pairs or -- better for the product -- stop making pairwise ordering claims and render simultaneous confidence bands (Tukey-style, or a bootstrap max-t band over the 11 categories), which is what a radar chart should carry anyway; (2) for the leaderboard, control FDR across the displayed comparison set rather than FWER, and publish the correction used; (3) add the detector's alpha to the same budget, since it is a hypothesis test evaluated on every submission. Every one of these changes widens intervals, so it interacts with the sizing tables in 07-statistics.md and must be applied before the ADR-0008 family budgets are re-derived, not after.

**Verifier.** The DEFECT is real and novel - a grep for bonferroni/holm/sidak/multiplicity/family-wise/FWER/FDR/per-comparison across all 34 documents returns zero hits, while 07:293 explicitly licenses within-model category comparisons and 07:294 licenses model-vs-model, all at alpha 0.05. No prior round touches it. But BOTH headline numbers are wrong and I could not reproduce either. (a) 1-0.95^55 = 94.05% assumes independent comparisons. The 55 pairs share means and are strongly positively dependent; simulating 11 independent category means with all-pairs z tests over 200,000 draws gives FWER 67.6% at per-comparison 5% and 82.4% at 8.5% - not 94.05%/99.24%. (b) The design already forbids the comparisons that make up 45 of the 55 pairs: 07:243-244 states 'Probe category scores are never ranked... A +/-19.6% error bar cannot support an ordering and should not be presented as if it could', and REVIEW.md S3 defines probe as 'explicitly directional-only, never ranked'. The licensed radar set is the 5 core categories = 10 pairs, where simulated FWER is 28.6% at 5% and 41.9% at 8.5%, with 0.50 expected false orderings, not 2.8. REVIEW.md S3's own motivating example, 'better at traits than at lifetimes', is a core-core comparison. So 'the first radar chart the project publishes will, with near-certainty, contain at least one category ordering that is not real' is false as stated (28.6%). Survives at moderate on the corrected arithmetic, which still shows a real uncorrected multiplicity problem, largest on the leaderboard.

**Verifier correction.** Replace every number. Within-model radar: 5 rankable core categories, 10 pairs, FWER 28.6% at a correctly-sized 5% and 41.9% at the 8.5% the specified estimator actually delivers; expected false orderings 0.50 and 0.85. Leaderboard: a 10-model board makes 45 x 5 = 225 category comparisons (E[false] 11.25) and a 20-model board 190 x 5 = 950 (E[false] 47.5) - not 495 and 2,090. Note explicitly that E[false] = k x alpha is exact under any dependence structure but FWER is not, and that using 1-(1-alpha)^k on pairwise comparisons of shared means overstates FWER by roughly 1.4x. Drop the 'near-certainty' framing. The resolution (Holm over the licensed pairs, or simultaneous max-t bands on the radar, or FDR across the displayed leaderboard set) stands unchanged and is still worth doing.

---

### `percentile-bootstrap-undercovers-at-small-cluster-counts` — The specified percentile cluster bootstrap under-covers at every per-category cluster count -- 92% at a core category, 84% at idiom-refactor -- and doubles the type-I error of the ranking claim

**SEVERE** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/13-architecture.md`, `docs/12-schemas.md`

**Defect.** docs/07-statistics.md:268-273 specifies a plain percentile cluster bootstrap: resample clusters with replacement, report the 2.5th and 97.5th percentiles. It carries no small-cluster correction -- no t-multiplier, no G/(G-1) scaling, no BCa, no studentisation, no cluster-wild variant. The nonparametric cluster bootstrap is a large-G procedure; the design applies it at G = 16 (a core category's shapes, per R4-S5), G = 12 (a probe category), and G = 5 (idiom-refactor's measured transform catalogue, per R5-S3). This is ORTHOGONAL to R5's `two-level-bootstrap-undercovers`: it persists when the shape level is modelled perfectly, because it is a property of the resampling method, not of the clustering model.

**Failure scenario.** Simulating the exact specified procedure against a correctly-specified shape -> family -> seed generating process with ICC_family 0.30 and ICC_shape 0.10, 3000-4000 trials each, coverage of a nominal 95% interval measures: 94.8% at 152 shapes (the whole corpus -- correct), 92.7% at 20, 92.0% at 16 (a core category, the tier the leaderboard ranks on), 90.8% at 12 (a probe category), and 83.9% at 5 (idiom-refactor). Misses are roughly symmetric, so the intervals are too NARROW rather than mis-centred. The consequence for the actual leaderboard claim: the type-I error of "model A differs from model B on this core category", made with the specified paired cluster bootstrap on two identical models, measures 7.9% at G=16 and 9.1% at G=12 against a nominal 5% -- 1.6 to 1.8x. Every per-category CI the project publishes is labelled 95% and is 84-92%.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/boot_cov.py (coverage) and boot_fix.py parts (A) and (B) (corrections and type-I error).

**Resolution.** The fix is cheap and was measured to work. Scaling the percentile interval about the point estimate by t_{G-1,0.975}/1.96 * sqrt(G/(G-1)) restores coverage to 94.7-95.1% at every G tested (the factor is 1.011 at G=152, 1.123 at G=16, 1.173 at G=12, 1.584 at G=5). A cluster-wild bootstrap-t with Rademacher weights restores 94.6-96.0% and is the more standard answer. Specify one of them in 07-statistics.md's CI-computation block, and add the coverage check to the bench-stats property-test list in 13-architecture.md:102, which currently says only "property tests against synthetic data with known ICC and known effect sizes" -- known ICC is not the same as verified coverage, and no round has checked coverage.

**Verifier.** CONFIRMED three ways. Spec text verified: 07:268-273 is a plain percentile cluster bootstrap ('report 2.5th and 97.5th percentiles') with no t-multiplier, no G/(G-1), no BCa, no studentisation. My own simulation used a different outcome shape (logistic-squashed bounded outcome, shape->family->seed, ICC_shape 0.10 / ICC_family 0.30, 4000 trials, 800 bootstrap reps) and reproduced within ~1pp at every G: 94.2% at 152, 93.1% at 20, 91.7% at 16, 91.3% at 12, 84.7% at 5, against the reviewer's 94.8/92.7/92.0/90.8/83.9. Both match exact theory: the percentile bootstrap of a mean is the normal interval with the /G plug-in SD, so coverage = P(|t_{G-1}| < 1.96*sqrt((G-1)/G)) = 0.948/0.931/0.923/0.913/0.846. Their correction factor is exactly t_{G-1,.975}/1.96 * sqrt(G/(G-1)) (1.011/1.096/1.123/1.173/1.584) and restores 94.4-94.9% in my sim, so the proposed fix is verified, not asserted. Genuinely orthogonal to R5's two-level-bootstrap-undercovers: this persists when the shape level is modelled perfectly. Severity stays severe - it silently makes every published per-category 95% interval an 84-92% interval and inflates the type-I error of the leaderboard's central ranking claim by ~1.6x, and 13-architecture.md:102's test plan ('known ICC and known effect sizes') cannot detect it.

**Verifier correction.** Two caveats to add. (1) The G=5 row overstates: 07:88-90 already declares that idiom-refactor's clustering is crossed and 'no nested bootstrap is valid for it' (Q24 open), so 83.9% is coverage for an estimator the design has already withdrawn there - state the finding on G=11-16 (borrow-lifetimes publishes shapes: 11; unsafe-core measured ~16) and cite G=5 only as an illustration. (2) The magnitude is conditional on the bootstrap unit, which the same document contradicts itself about: at the family level still written at 07:266-272 a core category has G=40 and coverage is ~94%, a 1pp miss. So this finding and bootstrap-unit-contradiction-within-07 must be fixed together, and the coverage claim should be stated as 'once the shape-level unit at 07:84 is actually implemented'.

---

### `R6-S2` — The per-category context gate has two incompatible thresholds and a probe that measures the wrong quantity; applied consistently it refuses every run at the configuration the harness itself recommends

**SEVERE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/04-categories.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/05-hardware-and-calibration.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/OPEN-QUESTIONS.md`

**Defect.** Three defects in one mechanism, all created by round 5's per-category gate. (a) The threshold is defined two ways 32,768 tokens apart: 04 §context-gate and 15 §2.4 derive `cross-module`'s `min_effective_ctx` as *source tokens + 32k completion budget = ~90k* (prompt **plus** budget), while 05's pre-run gate table says "Model context window smaller than the largest family's **prompt**" and "Effective context … below the suite's largest **prompt** → refuse" (prompt only, no budget term). (b) The probe measures neither: both 05 and 15 §9.2 probe 2 binary-search *prompt length* until the server 4xx's or `usage.prompt_tokens` stops tracking — on a `-c 32768` server that returns 32768, but the binding constraint is prompt + `max_tokens`. (c) 05 is absent from doc 15 §11.9's list of forced edits, so its **suite-level** refuse gate — which 15 §2.4 explicitly replaced — is still live and, evaluated at the suite's largest prompt (~90k), refuses essentially the whole 8–24 GB target band.

**Failure scenario.** Apply 04/15's own arithmetic (prompt + 32,768 budget) to a core category with a 2,000-token prompt: `min_effective_ctx` = 34,768. `rustybench doctor`'s documented healthy output (15 §9.2) reports `effective_ctx 32768`, `max_tokens 32768`, and "categories ok: 5 core" — arithmetically impossible; all five core categories exceed 32,768. 15 §2.4 says "The gate refuses a *run* only when a **core** category is unattemptable", so the harness must refuse. The user then follows the harness's own remediation (`-c 32768` / `OLLAMA_CONTEXT_LENGTH=32768` / `--max-model-len 32768`) and is refused again. Separately this falsifies 05:91 and 00:49 — "`capability_score_core5` always [is comparable], because those five categories are never machine-gated": at the recommended configuration all five are gated by the same mechanism.

**Evidence.** docs/04-categories.md:92-99 (per-category gate, ~90k = 62k source + 32k completion); docs/15-profiles-and-divisions.md:99 (same derivation), :484 (probe = binary-search prompt length), :501-508 (healthy doctor output: 32768 effective, max_tokens 32768, 5 core ok), :514-526 (remediation text), :673-687 (edit list — 05 not named); docs/05-hardware-and-calibration.md:141,144 (prompt-only thresholds, suite-level refuse), :164-167 (probe); docs/OPEN-QUESTIONS.md:132-140 (Q12 closed on this gate).

**Resolution.** Define `min_effective_ctx(category) = p99_prompt_tokens(category) + budget_tokens + repair_turn_growth` once, in 04, and make 05 and 15 reference it rather than restate it. Change the probe to measure the *admissible* `prompt + max_tokens` pair (send a probe request carrying the real `max_tokens`), not maximum prompt length. Delete 05's suite-level refuse rows, which 15 §2.4 superseded. Note that `repair` is the default mode and attempt 2's prompt contains attempt 1's full completion, so the requirement is roughly `prompt + 2 × budget_tokens` on the worst case.

**Verifier.** CONFIRMED, and the sharpest item in the set. (a) verified verbatim: 04:92-99 and 15:99 derive min_effective_ctx as source tokens PLUS the 32k completion budget (~90k), while 05:141 refuses on 'the largest family's **prompt**' and 05:144 on 'the suite's largest **prompt**' — no budget term, and one row is per-family while the other refuses the whole run. (c) verified: 15 §11.9's edit table (15:673-687) names ADR-0005, 06 x2, 08 x4, 02, 03, 04, 12 and 11-for-redaction. 05 is absent, so its suite-level refuse — the exact gate 15 §2.4 exists to replace — is still live, and evaluated at ~62k it does what 15:99 says must not happen ('marks `deep` invalid for the entire 8-24 GB band the project targets'). The failure scenario reproduces exactly: with a 2,000-token core prompt, min_effective_ctx = 34,768, while doctor's documented healthy block (15:501-508) prints 'context 32768 effective', 'max_tokens 32768' and 'categories ok: 5 core' in the same eight lines. That is arithmetically impossible by exactly 2,000 tokens, and the remediation block eight lines further down (15:520-522) reprints 32768 as the fix. Severe holds because the ambiguity is silent where it matters: two implementers reading 04 vs 05 produce different `categories_scored` for the same machine, and per R6-S3 `categories_scored` is the denominator of the sort key.

**Verifier correction.** Two corrections before this ships. Sub-claim (b) is REFUTED: the binary search over prompt length returns the largest admissible prompt, which on every named backend is n_ctx / max_model_len — exactly the quantity a `n_ctx >= prompt + budget` gate consumes. The probe measures the right thing; the gate is what is stated two ways. Drop 'a probe that measures the wrong quantity' from the title and drop the 'send a probe request carrying the real max_tokens' resolution. Also soften the 05:91 / 00:49 limb: a core category over budget refuses the *run* (15:99), so no published row ever carries an incomplete core5 and the 'always share a denominator' invariant survives — only the loose wording 'never machine-gated' is falsified. Keep the repair-turn note in the resolution; attempt 2's prompt containing attempt 1's completion is a genuine uncosted term.

---

### `R6-S3` — Round 5's denominator fix turned a passive machine-dependence into a submitter-chosen one worth +5.0 points, and never reached the document that specifies the leaderboard

**SEVERE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/11-submission-and-privacy.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/04-categories.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/06-execution-classes.md`

**Defect.** R5's `capability-score-denominator` found a 2–4 point machine-dependent shift on the sort key. The fix (04, 07, 15 §10.1) was to publish `categories_scored`, put it in the row key, and name `capability_score_core5` the cross-machine key. That fix is documentation-only and makes the exploit worse. 11-submission-and-privacy.md — the document that defines what is uploaded and how rows are rendered — was not edited: `categories_scored` is not in the "Always (public leaderboard)" upload list, `capability_score_core5` and `capability_score_synth` are not there either, and the "Leaderboard presentation rules" make `exec_class` a hard filter and quantisation equal-prominence while saying nothing about the denominator. Doc 15 §11.9's edit list touches 11 only for redaction. Meanwhile both skip paths are free, submitter-set knobs: `-c` (drops `cross-module`) and battery / declared `CpuOnly` (drops `perf-optimization`, 05:139, 06 §Where CPU genuinely hurts, 03:234-240). Doc 15 §5.4 refuses a *smaller* denominator attack — "**The denominator does not move.** … exclude the hardest 15% … **x = 46.2%, +6.2 points**" — while 04 mandates a comparable one on categories.

**Failure scenario.** Anchored on the design's own `report.json` example (capability_score 0.412, borrow-lifetimes 0.31), an 11-category vector averaging 0.4118: relaunch the server at `-c 32768` so `cross-module` emits `skipped_context` → 0.4380 (**+2.62 pts**); unplug the laptop so `perf-optimization` emits `perf_unavailable` → 0.4622 (**+5.04 pts**), range +3.4 to +6.6 over plausible per-category scores. `categories_scored` is published honestly; nothing in 11 keeps the row out of the same sorted table. The inversion is worse than the attack: the same model on a 128k-context machine scores **2.62 points lower** than on a 32k machine because it ran more of the benchmark. And since 15 §2.4 states the ~90k `cross-module` requirement is out of reach for "the entire 8–24 GB band the project targets", ADR-0004's decision to put `cross-module` in the headline — taken to stop the benchmark "biasing toward toy tasks" — is nullified for essentially the whole audience.

**Evidence.** docs/11-submission-and-privacy.md:18-32 (upload list: no categories_scored, no core5/synth), :105-115 (presentation rules: exec_class hard filter, denominator absent); docs/04-categories.md:129-156 (denominator rule, core5); docs/15-profiles-and-divisions.md:357 (§5.4 "The denominator does not move", +6.2 pt attack refused), :594-604 (row key), :686 (11 edited for redaction only); docs/05-hardware-and-calibration.md:139; docs/06-execution-classes.md (perf gated to GpuFull + AC). Arithmetic reproduced independently.

**Resolution.** Make the ranked table a function of a fixed denominator, not a published one: rank on `capability_score_core5` (or a fixed 11-category mean with skipped categories scored as `absent` and the row marked incomplete-not-rankable), and demote the variable-denominator `capability_score` to a displayed, non-sortable column. Add `categories_scored`, `division`, `capability_score_core5` and `capability_score_synth` to 11's upload list and add a presentation rule making the denominator a hard filter exactly like `exec_class`. Require `completeness < 1.0`-style rendering for any row missing a category.

**Verifier.** CONFIRMED. Arithmetic reproduced independently: dropping a category scoring s from an 11-category equal-weight mean m shifts the mean by (m-s)/10, so anchored on 12's own report.json (capability_score 0.412) a cross-module score of ~0.15 gives +2.62 points and a further perf-optimization score of ~0.22 gives +5.04; the stated envelope +3.4 to +6.6 corresponds to the two hardest categories averaging 0.115-0.259 against a 0.412 mean, which is plausible. Both skip paths verified submitter-controlled: `-c` sets effective_ctx and drops `cross-module` (15:502 shows the harness doing exactly this in its own worked example), and 05:139 'On battery power | warn; refuse for perf-optimization' plus 06:96 'execution class GpuFull only' drop the other. 11 verified: the 'Always (public leaderboard)' upload list (11:18-32) carries 'aggregates: capability_score, category scores, CIs, effective N' and neither `categories_scored`, `capability_score_core5`, `capability_score_synth` nor `division`; the presentation rules (11:105-116) make `exec_class` a hard filter and say nothing about the denominator. 15 §11.9 touches 11 for redaction only. The asymmetry with 15 §5.4 is real and well-put: the document refuses a +6.2-point denominator attack ('**The denominator does not move.**', 15:357, arithmetic checked: 0.85x+0.15(0.05)=0.40 -> x=46.2%) while 04 mandates a comparable one at category granularity. The inversion — same model scores 2.62 points lower on a 128k machine than on a 32k one — follows directly. Novel against R5's `capability-score-denominator`, which framed this as *machine*-dependent; the round-5 fix plus 15 §2.4 turned it into a *chosen* knob.

**Verifier correction.** One accuracy fix: doc 15 does specify the partition — `categories_scored` is a row-key component (15:597) and §10.1 says 'two rows may be *compared* when their keys match'. The defect is that the document specifying the rendered table and the upload payload (11) carries neither the field nor the rule, so the row key exists only in prose. State it that way rather than as 'nothing in the design keeps the row out'. Also note 04:151 already asks for `completeness < 1.0`-style rendering of a missing category, so that half of the resolution is a propagation, not a new rule. Finally, 15:99's 'entire 8-24 GB band' is the doc's own overstatement (a 24 GB card can hold a small Q4 model plus ~90k of KV); inherit it with attribution rather than as fact.

---

### `R6-S4` — Five of the nine row-key components are never transmitted — including `division`, so Open rows are indistinguishable from Pinned rows at ingest

**SEVERE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/12-schemas.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/11-submission-and-privacy.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`

**Defect.** Doc 15 §10.1 defines the row key as `suite_version · prompt_pack_hash · sampling_profile_id · division · weights_id · template_fingerprint · reasoning_mode · exec_class · categories_scored`. The on-wire submission manifest (12-schemas.md) carries a **different, smaller** `identity` block — `{suite_hash, generator_commit, harness_version, binary_hash}` — plus `model: {"…": "as in state.json"}`, where state.json's `model` sub-object is only `{name, quant, backend, backend_version, ctx, ngl}`. The new fields (`division`, `profile_hash`, `template_fingerprint`, `reasoning_format`, `tools_offered`, `categories_scored`, `exec_class_source`) are *siblings* of `model` inside state.json's `identity`, not inside it, so they are not carried. 11's upload list independently omits `prompt_pack_hash`, `sampling_profile_id`, `division`, `template_fingerprint`, `reasoning_mode`, `categories_scored`. `division` occurs exactly once in the entire repo outside doc 15: `12-schemas.md:55`, in the local `state.json`. Doc 15 §11.9 asks 12 to add these to `identity` but names the state.json block, not the manifest.

**Failure scenario.** A submitter runs `rustybench run --division open --endpoint <url>` on KoboldCpp with `-ctk q4_0`, rope scaling on, and a hand-edited LM Studio template — every gate failed, which Open is designed to absorb. `rustybench submit` serialises the manifest; the server receives no `division` field and no `template_fingerprint`. The row is ingested indistinguishably from a Pinned row and enters the ranked table. ADR-0010's central naming rule — "the unqualified phrase 'Rustybenchmark score' means **Pinned** (MLPerf naming rule)" — and SPEC Rule 4.1.5 ("a Tuned row may not be published without its Pinned twin") are both unenforceable, because the field that distinguishes the divisions never crosses the wire. The server also cannot compute the row key at all, so §10.1's comparability predicate and §10.3's within-backend default view have nothing to key on.

**Evidence.** docs/12-schemas.md:44-63 (state.json identity, `division` at :55), :205-231 (submission manifest: identity = 4 fields, model = as in state.json); docs/11-submission-and-privacy.md:18-32; docs/15-profiles-and-divisions.md:594-598 (row key), :685 (edit request aimed at state.json's identity block).

**Resolution.** Promote the manifest's `identity` block to carry the full row key verbatim, and make it the same struct as `state.json.identity` rather than a hand-listed subset. Add all nine key components to 11's upload list. Add a CI check that asserts every field named in 15 §10.1 appears in both 11's upload list and 12's manifest — the mechanical drift check R4-S6/R5-S7 introduced covers schema fields, not row-key membership.

**Verifier.** CONFIRMED on its load-bearing claim. I grepped the whole repo: `division` as a field occurs exactly once outside doc 15 — `docs/12-schemas.md:55`, inside `state.json.identity`. It is absent from the on-wire manifest (12:210-229), from report.json, from journal lines, and from 11's upload list. The manifest's `identity` is four fields {suite_hash, generator_commit, harness_version, binary_hash} and its `model` is `{'...': 'as in state.json'}`, which resolves to state.json's `identity.model` = {name, quant, backend, backend_version, ctx, ngl} — the new fields are siblings of `model`, not members, so they do not travel. 15 §11.9's edit line for 12 (15:685) says 'identity block gains division, profile_hash, template_fingerprint, tools_offered, categories_scored' without disambiguating which of 12's two `identity` blocks, and it landed on the local one. Exactly the R5-S7 `leaderboard-publishes-seeds` shape (fix applied to one block, missed the sibling), one round later, on the field that distinguishes the divisions. Consequences hold: without `division` on the wire, ADR-0010's naming rule and SPEC Rule 4.1.5 ('a Tuned row may not be published without its Pinned twin') are unenforceable at ingest, and the server cannot compute §10.1's key at all.

**Verifier correction.** Fix the membership claim. `categories_scored` IS transmitted — the manifest carries `report: {'...': 'report.json'}` and report.json:177-179 lists it — and `exec_class` arrives via `calibration: ['per-segment calib summaries']`, since calib.json:162 carries `exec_class`. The five genuinely absent from the manifest are `division`, `template_fingerprint`, `reasoning_mode`, `prompt_pack_hash` and `sampling_profile_id`. The count survives; the membership does not. Also the CI check in the resolution should assert against the *manifest* specifically, since the drift here is between two same-named blocks inside one file.

---

### `R6-S5` — The three-source `exec_class` rule turns one free-text string into the leaderboard's hard filter, a row-key component, the throughput comparison scope, and part of the capability denominator — with contradicting evidence in the same submission and no check

**SEVERE** · *verifier lowered severity* · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/06-execution-classes.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/11-submission-and-privacy.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/adr/0010-pinned-tuned-open-divisions.md`

**Defect.** ADR-0005 required `exec_class` to be "derived from the backend's reported layer split, never self-declared". ADR-0010/06/15 §2.4 replaced that with `observed | host | declared`, and "a `declared` exec class is leaderboard-eligible at the self-reported tier". Doc 15 §5.2 classifies `exec_class` at **S4** — "type a string", the weakest sub-tier, "free text, no anchor". That one string now controls four independent things: (a) a row-key component (15 §10.1); (b) a **hard leaderboard filter** — 11 says "Execution class is a hard filter, not a column to be scanned past"; (c) the comparison scope for `throughput_score`, `time_to_first_pass` and `efficiency_score` (05:91, 06); (d) whether `perf-optimization` is scored at all (06 gates it to `GpuFull`), i.e. part of the capability denominator. The submission already contains the contradicting evidence — `hw.json` GPU inventory and VRAM, `offload_ratio`, and calibrated `tg128`/`pp512` — and nothing compares them. R5-S5 killed the plausibility suite, so no other check remains.

**Failure scenario.** A submitter on a 24 GB RTX 4090 running `-ngl 99` types `exec_class = "CpuOnly"` in `model-profile.toml`. Consequences, all simultaneous and all free: `perf-optimization` is gated out of the denominator (+~1.9 points on `capability_score`, per R6-S3's arithmetic); the row lands behind 06's `CpuOnly` toggle where the population is thin, so a coarse row key plus a sparse filter makes it rank #1 by construction; `throughput_score` at ~40 tok/s and `efficiency_score` are published as the best CpuOnly figures on the board by roughly an order of magnitude, and 06 forbids cross-class comparison so nothing contradicts them; and `hw_public.hw_class = "vram-24"`, `vendor: nvidia`, sitting in the same signed manifest, is never cross-checked against the declaration. No document specifies a minimum population for a row-key cell, so the empty-cell win is not detected either.

**Evidence.** docs/06-execution-classes.md (three-source table; `declared` eligible; perf gated to GpuFull; timing class-scoped); docs/15-profiles-and-divisions.md:101 (three-source resolution), :284-296 (`exec_class`/`exec_class_source` in `[serving]`, `tier = self_reported`), :342 (S4 = "type a string"), :597 (exec_class in row key), :612 (S4 display bucket); docs/11-submission-and-privacy.md:111 (hard filter), :19-27 (hw_public shipped in the same payload); docs/adr/0010:Consequences ("Pinning is unenforceable").

**Resolution.** Add the one cross-check the data already supports: a `declared` `exec_class` of `CpuOnly` or `Hybrid` on a host whose `hw.json` reports an accelerator with `mem_total ≥ model_size × quant` is internally inconsistent — flag it and drop the row to `exec_class_source = "declared_contested"`, which is excluded from throughput ranking. Decouple the four uses: keep `exec_class` in the row key (capability), but scope throughput/efficiency ranking to `observed` or `host` sources only, and gate `perf-optimization` on the *measured* timing precheck (03:234-240) rather than on the declared class.

**Verifier.** SURVIVES but only on one of its four limbs; severe is not honest. Confirmed: ADR-0005's 'derived from the backend's reported layer split, never self-declared' was replaced by observed|host|declared with 'a `declared` exec class is leaderboard-eligible at the self-reported tier' (06:41-42); 15:342 does classify all `[serving]` fields, which includes `exec_class` (15:284), at S4 'free text, no anchor | type a string'; and it is simultaneously a row-key component (15:597), a hard leaderboard filter (11:112), the scope for throughput/ttfp/efficiency (05:91, 06:64), and the gate on `perf-optimization` (06:96). REFUTED limb: the capability-denominator gain buys nothing. `perf-optimization` is gated to `GpuFull` for the *entire* CpuOnly cohort, so every CpuOnly row shares that denominator and the +1.9 points is not a relative advantage; and 06:80-84 plus the row key already withdraw cross-class `capability_score` comparison. Worse for the attacker, 06:62 makes `GpuFull` the *primary* table — a false CpuOnly declaration moves the row off it. SURVIVING limb, and it is real: `throughput_score` and `efficiency_score` computed from GPU-speed execution and published as the CpuOnly class record, roughly an order of magnitude out, with 06:64 forbidding the cross-class comparison that would contradict it. Novel in cause (the derived->declared governance change is new in ADR-0010/15 §2.4), even though the missing-check consequence overlaps R5's `memory-physics-hybrid-hole`.

**Verifier correction.** Drop the capability/denominator limb and the '#1 by construction' framing (the prize is a secondary toggle view, not the primary table). Retitle around throughput: a self-declared `exec_class` scopes the throughput comparison, and nothing anchors it. Also fix 'no check remains': 10:148's memory-physics row plus the *measured* `peak_accel_mem_mb` (journal 12:112, calib 12:161) is precisely the anchor, and R5 already recorded that it is undefined for Hybrid/CpuOnly. Finally the proposed resolution is unsafe as written — 06:9 exists to serve people who legitimately run CPU-only MoE on machines that have accelerators, so 'GPU present => contest the declaration' is the R5-S5 firing-on-honest-users shape. Anchor the check on measured `peak_accel_mem_mb` near zero, not on `hw.json` inventory.

---

### `R6-S6` — The published `deep` wall clock is 6.9× low for exactly the population doc 15 pinned `max_tokens = 32768` to serve, and the 72-hour refuse gate is blind to it by construction

**SEVERE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/07-statistics.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/05-hardware-and-calibration.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/00-overview.md`

**Defect.** 07's timing model prices generation at 600 completion tokens. I reproduced its published figures exactly from that constant (per-unit @20 tok/s = (2000/200 + 600/20)×1.6 + 64 = 128 s; 1251 units → 44.5 h; @60 tok/s → 85 s → 29.7 h), which confirms the decomposition is the document's own. Doc 15 §2.2's *sole* justification for pinning `max_tokens` to 32768 is the measurement that Ornith-9B emitted 579 completion tokens to produce 113 characters of code — "~94% of emitted tokens were reasoning". Nothing propagated that number back into the timing model. `reasoning_mode` is a **detected** row-key field (15 §2.2, §4 `[reasoning]`), so the harness knows the answer at `doctor` time and still prints the 600-token estimate.

**Failure scenario.** A user runs `rustybench doctor` against a reasoning model at 20 tok/s. Doctor prints "Division: PINNED. Estimated 44.5 h for `deep`" (15 §9.2). At the document's own 94% reasoning ratio, 600 tokens of code require ~10,000 emitted tokens, giving 880 s/unit and **305.8 h** for `deep` — 6.9× the estimate; at 60 tok/s it is **116.8 h** versus the published 29.7 h (3.9×). If the model actually reaches the pinned 32,768-token cap the figure is 939 h. 05's pre-run refuse gate ("Calibrated `tg128` below the suite's floor (suite would exceed 72 h) → refuse") converts tok/s to hours through the same 600-token constant, so a reasoning model at 60 tok/s computes as 29.7 h and passes a gate it exceeds by 45 h. 00's "A 44.5-hour suite must survive being run in ninety-minute evening slices" becomes ~204 slices ≈ eight months of weeknights, which puts the entire reasoning-model population permanently across the epoch boundary R5 `epoch-boundary-resume` already flagged as unresolved.

**Evidence.** docs/07-statistics.md §Timing model and §Suites (600 completion tokens; 44.5 h / 29.7 h; 64 s token-bound + 64 s fixed); docs/15-profiles-and-divisions.md:61 (94% reasoning measurement), :504-508 (doctor prints max_tokens 32768 and 44.5 h in the same block); docs/05-hardware-and-calibration.md:137 (72 h refuse gate); docs/00-overview.md:Goals. Arithmetic reproduced independently.

**Resolution.** Make the timing model a function of `reasoning_mode`: publish two suite tables (reasoning / non-reasoning) with an explicit `reasoning_token_ratio` parameter, defaulting to the measured 0.94 until a better estimate exists. Have `doctor` compute the estimate *after* probe 8 (reasoning detection) rather than before, and feed the reasoning-adjusted figure into 05's 72-hour gate. State plainly that `deep` is not currently affordable for reasoning models on consumer hardware, and decide whether that is an accepted exclusion or requires a reasoning-specific suite tier.

**Verifier.** CONFIRMED, and I rebuilt it with a differently-shaped model. 07's decomposition reproduces exactly from the 600-token constant: (2000/200 + 600/20) x 1.6 = 64 s token-bound, + 64 s fixed = 128 s; 1088 x 128 x 1.15 (probe) = 44.5 h; at 60 tok/s 21+64 = 85 s -> 29.7 h. Confirmed that 15:61's sole justification for the `max_tokens` pin is the Ornith-9B measurement (579 completion tokens, 113 chars of code, '~94% of emitted tokens were reasoning') and that nothing propagated it into 07, 05:137's 72-hour refuse gate, or doctor's ETA (15:508 prints '44.5 h' four lines below 'max_tokens 32768'). `reasoning_mode` is detected at probe 8 (15:490), so the harness knows the answer and prints the wrong number anyway. Independent check on the multiplier: published measurements put reasoning models at >90% internal-reasoning share and 8-14x the output length of non-reasoning models (Qwen3-4B 14x, Qwen3-8B >10x; mean ~3,369 reasoning tokens/response with ~9.5% in the final answer). Rebuilding with an 8x/14x total-output multiplier instead of the finding's ratio inversion gives 161 h and 261 h respectively — 3.6x and 5.9x the published 44.5 h — bracketing the finding's 6.9x at the top. Direction, mechanism and the blindness of the 72-h gate all hold under both model shapes.

**Verifier correction.** Restate the headline as a range, not a point. '6.9x' comes from inverting the 94% ratio measured on a 35-token solution and assuming reasoning scales with solution length; that assumption is not established (reasoning length tracks problem difficulty, not output length). Publish it as '~3.5x to 7x, with the document's own 94% figure at the top of the range', and correspondingly soften '@60 tok/s exceeds the 72-hour gate by 45 h' to 'crosses the 72-hour gate somewhere between 8x and 14x output inflation, while the harness computes 29.7 h'. The 939 h at the pinned cap and the epoch-boundary consequence (which overlaps R5's `epoch-boundary-resume`) should be flagged as an upper bound and a pointer respectively.

---

### `standard-suite-overall-ci-self-contradiction` — docs/07-statistics.md publishes two different values, +/-6.5% and +/-5.7%, for the `standard` suite's overall CI, 35 lines apart -- two round-5 fixes collided

**MODERATE** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/04-categories.md`

**Defect.** The round-5 cleanup applied `tier-vs-standard-suite` (standard runs 208 families across EIGHT categories) and `overall-ci-wrong-estimator` (the headline is an equal-weight mean of category means) independently, and they contradict. The suite table at 07:151 gives `standard ... +/-CI overall = +/-6.5%`. The prose block at 07:183-185 gives `Var = (1/121) * Sum_c Var_c` and `standard: +/-5.7% (equal-weight effective N 298)`. The constant 1/121 is 1/11^2 -- hardcoded for eleven categories -- and is being applied to a suite that the SAME PAGE, 20 lines above, establishes runs eight.

**Failure scenario.** Recomputing from the document's own inputs (ICC 0.3, DE = 1 + (m-1)*ICC, Var_c = 0.25/eff_N_c, core 40 families, probe 12): at `standard` (2 seeds, DE 1.30) eff_N_core = 61.5 and eff_N_probe = 18.5. Over K = 8 (4 core + 4 probe, which is what 208 families is) the result is +/-6.50% with equal-weight effective N 227 -- matching the table row exactly. Over K = 11 it is +/-5.68% with equal-weight effective N 298 -- matching the prose block exactly, including the published 298. So the table is right and the prose is wrong by 0.8 percentage points, and the published effective N is overstated by 31%. The same defect propagates to the adjacent "lite (10 categories): ... +/-5.8% standard" figure, which reproduces at K = 10 and is likewise computed on a category set `standard` cannot run. A reader sizing a run, or a reviewer checking the arithmetic, gets a different answer depending on which of the two adjacent statements they read.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/recheck.py section 1, which reproduces both published numbers from their respective category counts.

**Resolution.** Correct 07:185 to `standard: +/-6.5% (equal-weight effective N 227)`, correct the `lite` standard figure the same way, and replace the hardcoded `(1/121)` in the formula at 07:183 with `(1/K^2)` where K = |categories_scored|, since the denominator is already established as part of the score's identity by the round-5 `capability-score-denominator` fix. The deep-row figures (+/-4.9%, equal-weight effective N 408) reproduce exactly and need no change.

**Verifier.** CONFIRMED exactly, by independent recomputation from the document's own inputs. At ICC 0.3, seeds 2, DE 1.30, core 40 families and probe 12: over K=8 (4 core + 4 probe = 208 families, which is what 07:167-168 establishes standard runs) the result is +/-6.50% with equal-weight effective N 227, matching the suite table at 07:154. Over K=11 it is +/-5.68% with equal-weight effective N 298, matching the prose block at 07:185 digit for digit, including the published 298. The hardcoded (1/121) at 07:183 is 1/11^2. The lite figures also reproduce from K=10 (+/-4.97% deep, +/-5.81% standard vs the published +/-5.0%/+/-5.8%), and the deep row reproduces at K=11 (+/-4.85%, effN 408). So the table is right, the prose is wrong by 0.8pp, and the published effective N is overstated by 31%. This is exactly the round-3/4/5 pattern: R5's overall-ci-wrong-estimator fix (which produced +/-5.7%) and R5's tier-vs-standard-suite fix (which produced +/-6.5%) were applied independently and collided. Neither round found the collision. Moderate is honest - standard is not the ranked tier.

**Verifier correction.** Add one more staleness the finding misses: 04-categories.md:140 records that `_lite` is itself superseded ('the old `_lite` excluded only cross-module'), replaced by capability_score_synth (9 categories) and capability_score_core5 (5). So 07:186's 'lite (10 categories)' line is stale on two counts - wrong K for standard AND a retired score name - and should be deleted rather than corrected, with _synth and _core5 figures published in its place.

---

### `design-effect-formula-never-updated-to-two-level` — 07 declares clustering two-level and makes the bootstrap resample shapes, but the design-effect formula was never updated -- every published effective N and CI, including round 5's new ones, still assumes zero shape clustering

**MODERATE** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/12-schemas.md`, `docs/adr/0008-core-and-probe-categories.md`, `docs/14-roadmap.md`

**Defect.** Round 5's fix made shape -> family -> seed clustering first-class in 07-statistics.md and changed the bootstrap unit. It did not touch 07:18, `design_effect = 1 + (seeds_per_family - 1) x ICC`, which is the one-level formula, and every effective-N and CI number in the document is still computed from it. The correct balanced three-level design effect is `1 + (m-1)*rho_f + m*(F-1)*rho_s`, where F is families per shape and rho_s the between-family-within-shape correlation. Setting rho_s = 0 recovers the published formula -- i.e. every published number assumes exactly the clustering the document says exists.

**Failure scenario.** docs/12-schemas.md:186 makes this concrete and self-contradictory in one line: `borrow-lifetimes` is published with `"n": 160, "effective_n": 84, "shapes": 11, "icc_within_family": 0.31, "icc_within_shape": null`. With 40 families over 11 shapes, F = 3.64 and m = 4, effective_n = 84 is reproducible only at rho_s = 0.00 (DEFF 1.930, eff N 82.9). At rho_s = 0.10 the correct value is 53.6 and the honest CI is +/-13.4%; at rho_s = 0.20 it is 39.6 and +/-15.6%. The "+/-10.7%, core category scores are rankable at deep" leaderboard policy holds only at rho_s = 0. The schema publishes the shape count and a null shape ICC beside an effective N that presumes the shape ICC is zero. The overall headline is more robust -- recomputing the equal-weight mean two-level gives +/-4.89% at rho_s=0, +/-5.34% at 0.10, +/-5.76% at 0.20 against the published +/-4.9% -- so the damage is concentrated exactly where the design makes its rankability claim. This is the fix-creates-the-next-hole pattern: round 5's newly published +/-4.9% is built on the design effect round 5's own R5-S3 invalidated.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/recheck.py sections 2 and 3.

**Resolution.** Replace 07:18 with the three-level form and mark every effective-N and CI figure in the document as conditional on rho_s, exactly as they are already conditional on ICC -- the ICC sensitivity grid should become a two-dimensional grid over (rho_f, rho_s). Give `icc_within_shape` a mandatory value in report.json rather than allowing null, since a null there silently means rho_s = 0 in the arithmetic. Phase 3.5 already has "estimate the shape component" on its list; the arithmetic that consumes it does not yet exist.

**Verifier.** CONFIRMED. 07:18 is still the one-level `design_effect = 1 + (seeds_per_family - 1) x ICC`, and every effective-N and CI figure in the document derives from it. I re-derived the balanced three-level design effect from scratch - Var(ybar) = [Fm*sigma_a^2 + m*sigma_b^2 + sigma_e^2]/(GFm) gives DEFF = 1 + (m-1)*rho_f + m*(F-1)*rho_s - and the finding's formula is correct. The schema arithmetic reproduces: borrow-lifetimes at 40 families / 11 shapes (F = 3.636) / m = 4 gives DEFF 1.930 and effective_n 82.9 at rho_s = 0 (published 84, which is the ICC 0.30 value while the same row publishes icc_within_family 0.31 - a second small inconsistency), 53.6 and +/-13.4% at rho_s = 0.10, 39.6 and +/-15.6% at rho_s = 0.20. The self-contradiction in 12-schemas.md:185 is real and is the sharpest evidence: the row publishes `shapes: 11` and `icc_within_shape: null` beside an `effective_n` that presumes the shape ICC is exactly zero. Novel - R5 changed the bootstrap unit and left the arithmetic that feeds every published number untouched.

**Verifier correction.** One qualification: 07:75-79 already concedes part of this ('no core category currently reaches the figure ADR-0008 claims'; budgets 'marked provisional pending a shape-count audit per category (Q24)'), so 'the +/-10.7% rankability claim holds only at rho_s = 0' is partly already on the record. State the finding on what is NOT conceded: 07:18's formula, and the fact that 07:155 and 07:239 still publish +/-10.7% and 'core category scores are rankable at deep' twenty lines after 07:78 says the figure is unreachable - so the document asserts and withdraws the same number. Also note the overall-headline recomputation is assumption-sensitive: I get +/-5.15% to +/-6.05% at rho_s = 0.10 depending on the assumed shapes-per-probe-category, against the finding's +/-5.34%, so publish that figure as a range conditional on the probe shape count rather than a point.

---

### `bootstrap-unit-contradiction-within-07` — The CI-computation section an implementer builds from still specifies family-level resampling, contradicting the same document's shape-level fix 180 lines earlier

**MODERATE** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/13-architecture.md`

**Defect.** docs/07-statistics.md:84-85 states "the bootstrap resamples **shapes**, carrying their families and seeds". docs/07-statistics.md:266-278 -- the section headed "Confidence interval computation", which is the procedural spec `bench-stats` implements from -- still reads "**Cluster-bootstrap at the family level**", "resample task families with replacement", "carry all seeds of each resampled family along", and closes with "The same clustering applies to category scores (resample families within category) and to paired comparisons (resample families, recompute the paired difference)." Round 5's fix was applied to the discussion and not to the specification.

**Failure scenario.** An implementer reading 13-architecture.md:48 ("bench-stats -- cluster bootstrap ... pure functions over the journal") goes to the CI-computation block for the algorithm and implements family-level resampling, which is precisely the estimator R5-S3 declared under-covering. Nothing downstream catches it: the schema field `shapes` is populated by a different code path, the CI is not cross-checked against anything, and the bench-stats test plan in 13-architecture.md:102 tests "known ICC and known effect sizes", neither of which is sensitive to the resampling unit. The result is a silently reintroduced defect that a full review round already found and believed fixed.

**Evidence.** Direct text comparison, docs/07-statistics.md:84-85 against docs/07-statistics.md:266-278.

**Resolution.** Rewrite the CI-computation pseudocode block to resample shapes, and update the three trailing clauses (category scores, paired comparisons) to match. While there, note the unresolved case the document already flags at 07:88-90: `idiom-refactor`'s clustering is crossed rather than nested, so neither unit is valid for it -- the pseudocode should say what it does for that category rather than leaving the only stated algorithm silently inapplicable to one of the five core categories.

**Verifier.** CONFIRMED by direct text read, no computation needed. 07:84-85: 'the bootstrap resamples **shapes**, carrying their families and seeds'. 07:266: '**Cluster-bootstrap at the family level. Never resample instances independently.**' 07:271-272: 'resample task families with replacement / carry all seeds of each resampled family along'. 07:278: 'The same clustering applies to category scores (resample families within category) and to paired comparisons (resample families, recompute the paired difference).' The procedural block an implementer builds from - and which 13-architecture.md:48 points bench-stats at - still specifies the estimator R5-S3 declared under-covering. Round 5's fix reached the discussion and not the specification. Textbook instance of the fix-creates-the-next-hole pattern the brief asked me to look for, and nothing downstream would catch it: 13-architecture.md:102's property tests are on 'known ICC and known effect sizes', neither of which is sensitive to the resampling unit. Moderate is honest.

**Verifier correction.** None needed. Worth pairing explicitly with percentile-bootstrap-undercovers-at-small-cluster-counts in the write-up, since the two interact: fixing this one (moving to shapes, G = 11-16) is what makes the small-G under-coverage bite, so the rewritten pseudocode block should carry the small-G correction in the same edit rather than in a later one.

---

### `icc-estimator-unspecified-and-unbounded-below` — No ICC estimator is specified anywhere, and the natural one returns negative values often enough to publish CIs tighter than an independent sample

**MODERATE** · surface: `bench-stats` · docs: `docs/13-architecture.md`, `docs/12-schemas.md`, `docs/07-statistics.md`

**Defect.** docs/13-architecture.md:48 assigns `bench-stats` "ICC estimation" with no definition; docs/12-schemas.md:186 carries `icc_within_family` and `icc_within_shape` with no stated domain; docs/07-statistics.md never names an estimator. For a continuous bounded outcome the natural choice is the one-way ANOVA estimator ICC = (MSB - MSW)/(MSB + (m-1)MSW), which is unbounded below. Nothing in the design truncates it at zero, and `design_effect = 1 + (m-1) x ICC` is then less than 1, making `effective_N` exceed the actual unit count.

**Failure scenario.** At the design's own assumed ICC of 0.30 on the observed scale, 20,000 trials each: a 12-family probe category at `standard` (2 seeds) draws a negative estimate 17.73% of the time; at `deep` (4 seeds) 4.07%; a 40-family core category at `standard` 3.55%. In the worst draws the design effect falls to 0.28-0.30, inflating effective N by ~3.5x and publishing a CI 1.84-1.88x TIGHTER than an iid sample of the same size. That is the precise failure the cluster bootstrap exists to prevent (07:274, "Naive per-instance bootstrap understates the CI by roughly 40%"), arriving through the other estimator in the same crate. Roughly one in six published probe-category rows at `standard` is affected, and nothing flags it -- the row simply looks unusually precise.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/icc2.py, final section.

**Resolution.** Name the estimator in 07-statistics.md, truncate at zero (max(0, ICC_hat)) as is standard practice, and give `icc_within_family` / `icc_within_shape` a documented domain of [0,1] in 12-schemas.md. Better: publish an interval for ICC rather than a point estimate, since at 12 families x 4 seeds the 95% range of the estimate spans [0.121, 0.791] when the truth is 0.50 -- a point value at that precision should not be feeding a design-effect calculation at all. Add "estimator returns a value in [0,1] and effective_N never exceeds N" to the bench-stats property tests in 13-architecture.md:102.

**Verifier.** Text CONFIRMED - 13-architecture.md:48 assigns bench-stats 'ICC estimation' with no definition, 12-schemas.md:185 carries icc_within_family and icc_within_shape with no stated domain, and 07 never names an estimator. Numbers REPLICATE under my own one-way ANOVA implementation and generative model, 20,000 trials per cell at true ICC 0.30: P(ICC_hat < 0) = 15.5% at F=12/m=2 (probe category at standard), 4.0% at F=12/m=4, 3.0% at F=40/m=2, 0.01% at F=40/m=4, against the reviewer's 17.73/4.07/3.55. Repeating on a logistic-squashed bounded outcome gives 15.9/4.5/3.3 - the rate is not an artifact of normality. Precision at true 0.50, F=12/m=4: 95% range [0.136, 0.730] against their [0.121, 0.791]. So roughly one in six probe-category rows at standard would publish a design effect below 1 and an effective_n exceeding n. Moderate is right.

**Verifier correction.** The stated consequence is wrong and must be replaced. 'Publishing a CI 1.84-1.88x TIGHTER than an iid sample' does not follow: 07:61-64 states 'The reported CI is always the cluster bootstrap, not this formula; the table exists for sizing decisions', and 07:274 repeats it. The published per-category CI is never computed from the design effect. What a negative ICC actually corrupts is (a) the published effective_n field in report.json, which becomes larger than n - visibly nonsensical but published, (b) gate G2, and (c) the Phase 3.5 re-sizing. Note one path that does reach a CI and contradicts 07:64: 14-roadmap.md:113 says P3.5 will 'Recompute suite sizing, family budgets, and every published CI from the measured value'. Also, the 1.84-1.88x figure is a deep tail, not the one-in-six case - at the 1st percentile of the F=12/m=2 distribution the effective-N inflation is 1.67x, giving a CI 1.29x too tight. State the finding as 'effective_n exceeds n on ~1 in 6 probe rows, and the sizing arithmetic inverts', not as a CI corruption.

---

### `R6-S7` — 07's corrected overall-CI block still assumes eleven categories at `standard`, so the document publishes ±5.7% and ±6.5% for the same statistic and overstates effective N by 31%

**MODERATE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/07-statistics.md`

**Defect.** Round 5 applied two fixes to 07 in the same pass. `tier-vs-standard-suite` established that `standard` runs **208 families across 8 categories** (all 40 `unsafe-core`, 12 `perf-optimization`, 12 `cross-module` are deep-tier). `overall-ci-wrong-estimator` recomputed the headline CI as an equal-weight category mean, `Var = (1/121)·Σ_c Var_c`. The second fix was computed against the *pre-fix* eleven-category `standard` suite — the literal `1/121` is 11², and the per-category effective Ns used are `standard`'s 2-seed values. The two fixes contradict each other inside one document, and the table and the prose disagree by 0.8 points.

**Failure scenario.** I recomputed both. Using `standard`'s DE = 1.30 with the **8** categories that actually run (4 core at eff N 61.54, 4 probe at 18.46): `Var = (1/64)·Σ = 0.0011003` → **±6.501%**, equal-weight effective N **227.2** — which is exactly the `±CI overall` the suite table prints for `standard`. Using **11** categories: `Var = (1/121)·Σ = 0.00083891` → **±5.678%**, effective N **297.8** — exactly the prose's "standard: ±5.7% (equal-weight effective N 298)". The prose figure is the refuted one. Any submitter or reader taking 07's stated equal-weight N of 298 gets a sample size 31% larger than the suite delivers. The same error repeats one line down: "lite (10 categories): ±5.0% deep, **±5.8% standard**" — at `standard`, `cross-module` is deep-tier and absent anyway, so `lite` and full `standard` are the identical 8 categories and the identical ±6.5%; I reproduce the ±5.8% only by assuming 10 categories run at `standard`. (`lite` is also stale: 04 replaced `capability_score_lite` with `_synth` and `_core5`.)

**Evidence.** docs/07-statistics.md §`±CI overall` uses the right estimator now ("deep: ±4.9% … standard: ±5.7% (equal-weight effective N 298) … lite (10 categories): ±5.0% deep, ±5.8% standard") against the `standard` row of the Suites table (±6.5%) and the paragraph immediately above it ("`standard` runs **208 families across 8 categories**, not 272 across 11"). Deep-suite values verified correct: 11-cat ±4.854%/407.6 vs published ±4.9%/408; core-cat ±10.68%/±19.50% vs published ±10.7%/±19.5%.

**Resolution.** Replace the hard-coded `1/121` with `1/k²` where `k = |categories_scored|`, and recompute the `standard` line as ±6.5% / equal-weight effective N 227. Delete the `lite` line or restate it per suite. Publish equal-weight effective N in the Suites table alongside the pooled `Eff. N` column so the two can never drift apart again.

**Verifier.** CONFIRMED to three decimal places, independently recomputed. With Var = (1/k^2) * sum_c (0.25 / eff_N_c), ICC 0.3: at `standard` (2 seeds, DE 1.30) the 8 categories that actually run (4 core at eff N 61.54, 4 probe at 18.46) give Var = 0.00110026, half-width +/-6.501%, equal-weight effective N 227.2 — which is exactly the '+/-6.5%' the Suites table prints for `standard`. The same suite evaluated over 11 categories gives Var = 0.00083936, +/-5.678%, effective N 297.8 — exactly the prose's 'standard: +/-5.7% (equal-weight effective N 298)'. So the two round-5 fixes contradict each other inside one document and the prose figure is the refuted one; effective N is overstated by 31% (298 vs 227). Deep-suite values verified correct: 11-cat +/-4.854% / 407.6 vs published +/-4.9% / 408; core cat +/-10.679% vs +/-10.7%; probe cat +/-19.498% vs +/-19.5%; pooled 572.6 vs 573. The `lite` line reproduces only at 10 categories (deep +/-4.971% -> '+/-5.0%', standard +/-5.815% -> '+/-5.8%'), and at `standard` a 10-category lite is the identical 8-category set, so both the 10 and the +/-5.8% are impossible. `lite` is also stale terminology — 04:142 refers to 'the old `_lite`'. Novel: round 5 created this collision (its own follow-up table prints +/-6.5% and +/-5.7% for the same statistic two rows apart), and no prior round names it.

**Verifier correction.** None material. If tightening: the literal `1/121` is the tell, and the fix is `1/k^2` with k = |categories_scored| — which also makes 07 consistent with 04's denominator rule instead of restating a constant beside it.

---

### `R6-S8` — 12's reference `report.json` uses three different denominators for three scores on one row — the exact defect the round-5 denominator fix was written to prevent

**MODERATE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/12-schemas.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/04-categories.md`

**Defect.** The example row's `categories_scored` lists **ten** categories — `perf-optimization` is absent, correctly reflecting the machine gate. But `capability_score` publishes `effective_n_equal_weight: 408` with `ci95: [0.363, 0.461]` (half-width 0.049), which I reproduce as the **eleven**-category figure (407.6, ±4.854%); the correct ten-category values are 388.7 and ±4.97%. `capability_score_synth` publishes 355 / half-width 0.051, which corresponds to **eight** categories (359.3, ±5.170%) — i.e. the nine synthetic categories minus the skipped `perf-optimization`. But 04 defines `_synth` as "mean over the **9** synthetic categories", a fixed denominator, with no `categories_scored` rule attached and no equivalent of the caveat 07 supplies for `_core5` ("undefined at `standard`"). `capability_score_core5` publishes 421 with half-width 0.049; 421 corresponds to ±4.776%, so its own two published numbers disagree.

**Failure scenario.** An implementer builds `bench-report` from 12-schemas.md's example, as the schema document exists to let them do. The resulting `report.json` reports a CI computed over a category set the run did not score — narrower than the truth by 0.12 points and, more importantly, silently wrong in a way no test would catch because the value is plausible. At `standard`, where `_synth` runs only 7 of its nominal 9 categories (both `unsafe-core` and `perf-optimization` are deep-tier), the fixed-denominator definition in 04 and the variable-denominator arithmetic in 12 diverge by two whole strata, and `_synth` inherits exactly the machine-dependence that `_core5` was created to escape.

**Evidence.** docs/12-schemas.md:176-184 (categories_scored lists 10; capability_score 408/±4.9%; _synth 355/±5.1%; _core5 421/±4.9%); docs/04-categories.md:129-133 (`capability_score_synth = mean over the 9 synthetic categories`), :145-153 (denominator rule applied to `capability_score` only); docs/07-statistics.md (`capability_score_core5` is undefined at `standard` — the parallel caveat `_synth` lacks). All values recomputed.

**Resolution.** Give `_synth` and `_core5` their own published denominator fields (`synth_categories_scored`, and a stated rule for `standard`), and require every score object in `report.json` to carry the category list its CI was computed over. Fix the example: 10-category `capability_score` is 388.7 / ±5.0%; `_core5` is 421 / ±4.8%; `_synth` at 8 categories is 359 / ±5.2%.

**Verifier.** SURVIVES on its headline, which I reproduced exactly. report.json:177-179 lists ten categories (`perf-optimization` absent) while capability_score publishes `effective_n_equal_weight: 408` and ci95 [0.363, 0.461]: the 11-category deep value is 407.6 / +/-4.854%, giving 0.412 +/- 0.04854 = [0.3635, 0.4605] -> [0.363, 0.461], an exact match; the correct ten-category values are 388.7 / +/-4.971% -> [0.362, 0.462]. So both published numbers are the eleven-category ones on a ten-category row. This is a collision of two round-5 fixes inside one JSON block — `report-json-stale` wrote the 408, `capability-score-denominator` wrote the ten-entry list — which is exactly the round-6 pattern and is novel. The durable design half also holds: 04:131 defines `_synth` as a fixed 'mean over the **9** synthetic categories' with no skip rule and no equivalent of 07's '`capability_score_core5` is undefined at `standard`' caveat, and at `standard` only 7 of the 9 run (both `unsafe-core` and `perf-optimization` are deep-tier), so `_synth` inherits the machine-dependence `_core5` was created to escape. `_core5` confirmed self-inconsistent: 421 implies +/-4.776% -> [0.353, 0.449], published [0.352, 0.450] = +/-0.049.

**Verifier correction.** The `_synth` attribution is wrong and must be replaced. Published `_synth` ci95 [0.386, 0.488] on 0.437 is the **nine**-category value (372.1 / +/-5.081% -> [0.3862, 0.4878]) matching to the last digit, NOT the eight-category one (359.3 / +/-5.170% -> [0.385, 0.489]); and `effective_n_equal_weight: 355` matches neither 9 (372.1) nor 8 (359.3). So the correct statement is that `_synth`'s own two published numbers disagree with each other — the same defect the finding correctly identifies for `_core5` — not that its CI reflects a skipped category. Restate the resolution's corrected figures as: 10-category capability_score = 388.7 / +/-4.97%; `_core5` = 421.1 / +/-4.78%; `_synth` at 9 categories = 372.1 / +/-5.08%. Also soften the failure scenario: a real `bench-report` computes the CI from the cluster bootstrap, not from these constants, so the harm is implementer confusion plus the undefined `_synth` denominator, not a silently wrong shipped number.

---

### `R6-S9` — 10-integrity.md still advertises OS-enforced 'no network during generation' after 08 moved the model call out of the sandbox, so a headline misconduct control now maps to a phase it does not cover

**MODERATE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/10-integrity.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/08-run-protocol.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`

**Defect.** Doc 15 §8.4 correctly identified that step 4 of 08's per-unit sequence made a loopback HTTP call inside a loopback-denying region, and 08 was fixed: step 3 is now "render prompt, send to backend … (HARNESS process, NOT sandboxed)" and the network policy heading reads "Zero network access during **grading** — the only phase in which model-authored code runs". 10-integrity.md was not touched. Its anti-gaming control table still reads "**No network during generation or grading** | OS-level (netns / seatbelt / job object), not policy. Attempted connection = hard fail", mapped to "Terminal-Bench case 3 (agent fetched solutions)", and its T2 section still asserts "The sandbox still denies network *during* every unit." Doc 15 §11.9's edit list names 08 twice for this change and does not name 10 at all — the same class of miss R5-S7 catalogued, one round later, on the security document.

**Failure scenario.** A reader — or an implementer, or a reviewer assessing the leaderboard's integrity claims — reads 10's control table and concludes that generation-time network access is blocked by the OS and that Terminal-Bench misconduct case 3 is structurally addressed. It is not: generation is now an unsandboxed outbound HTTP request from the harness process, by design and necessarily. The `agentic` mode listed in 08's interaction-mode table is precisely where generation-time network egress matters, and 10 offers no control for it. The claim "the sandbox still denies network during every unit" is now false for every unit in every mode, which also undermines the offline/batched-issuance argument built on it ("T2 requires network access *between* batches").

**Evidence.** docs/10-integrity.md:105 ("The sandbox still denies network *during* every unit"), :162 (control table row); docs/08-run-protocol.md:41-46 and :50-57 (corrected sequence and the explicit rationale), :78-80 ("Zero network access during grading"); docs/15-profiles-and-divisions.md:678-679 (edits forced on 08; 10 absent from the whole table).

**Resolution.** Rewrite 10's control row as "No network during **grading** — the only phase model-authored code runs — OS-level, attempted connection = hard fail", and state separately that generation is an unsandboxed harness HTTP call whose destination is unverifiable (which 15 §5.3 already concedes: "Whether the base URL proxies to a hosted frontier model — nothing in this design detects it"). Delete the "denies network during every unit" sentence. Add 10 to the drift check's set of documents that must be re-read whenever 08's sandbox boundary moves.

**Verifier.** CONFIRMED verbatim on both sides. 08 was fixed: step 3 now reads 'render prompt, send to backend, capture response ... (HARNESS process, NOT sandboxed -- see below)' (08:41), with an explicit rationale block ('Sandboxing that step protects against nothing and breaks the only thing it touches', 08:50-57) and a network-policy heading narrowed to '**Zero network access during grading** — the only phase in which model-authored code runs' (08:78). 10 was not touched: its anti-gaming control table still reads 'No network during generation or grading | OS-level (netns / seatbelt / job object), not policy. Attempted connection = hard fail' mapped to 'Terminal-Bench case 3 (agent fetched solutions)' (10:162), and 10:105 still asserts 'The sandbox still denies network *during* every unit.' I checked 15 §11.9's entire edit table (15:673-687): 08 appears four times, twice for exactly this change; 10 does not appear at all. So a headline misconduct control now names a phase it does not cover, in the document whose only job is to state what the integrity claims are. Moderate is honest — it does not corrupt a number, but it is a false security claim in the security document, and 15 §5.3 already concedes the matching limit ('Whether the base URL proxies to a hosted frontier model — nothing in this design detects it').

**Verifier correction.** Minor softening on one sentence: 10:105's 'denies network during every unit' sits in a paragraph about T2 needing network *between* batches, and the loopback model call is not external egress, so 'now false for every unit in every mode' overreads it. The control-table row at 10:162 is the unambiguous defect; lead with that and treat 10:105 as collateral wording. The `agentic` point is fair but note 15 §3.4 already caps that track at T0 on independent grounds.

---

### `R6-S10` — Pinning concurrency to 1 makes every published throughput and efficiency figure a single-stream number ~4× below what the machine delivers on the benchmark's own workload, and no document says so

**MODERATE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/00-overview.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/05-hardware-and-calibration.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/adr/0010-pinned-tuned-open-divisions.md`

**Defect.** Serving concurrency = 1 is a hard GATE (15 §2.2; 05 pre-run gate "refuse", "Non-negotiable for a ranked row") and the harness is a strictly serial client — 08's per-unit sequence issues one request, waits, grades, repeats. Continuous batching is the largest serving-side throughput lever there is: published llama.cpp slot benchmarking measures ~3.8× aggregate throughput over sequential decode, and the gain exists *only* under concurrent traffic. The benchmark's own workload — 1088 independent, idempotent work units (08: "Work units are **independent and idempotent**") — is embarrassingly parallel and is deliberately executed serially. Nothing in 00, 05, 06, 11 or 15 discloses that `throughput_score` and `efficiency_score` are single-stream figures. ADR-0010's stated consequence "the throughput half of the two-number thesis survives intact: threads (3.75×), batch/ubatch, flash attention, speculative decoding and all hardware stay free" omits that the ~3.8× lever is gated shut and that speculative decoding is OFF for Pinned by 15 §2.3.

**Failure scenario.** A reader takes the README at its word — "what a given machine + model + quantisation combination actually delivers per hour, per GB of VRAM, per watt-hour" — and sizes a batch job from a published `throughput_score` of 21.4 units/hour. The machine delivers roughly 4× that when the same 1088 independent units are run through 4–8 slots. `efficiency_score` (tasks/kWh) is distorted further and in the same direction: single-stream decode is memory-bandwidth-bound with the compute units idle, so batching amortises the same weight reads across sequences and cuts energy per token by a similar factor. Neither number can be recovered from any other division: `Tuned` frees concurrency, but the harness still issues one request at a time, so `--parallel 4` yields no throughput gain and `tuning_gain = throughput_tuned / throughput_pinned` measures ~1.0 for the largest lever there is. The quantity the two-number thesis names is therefore not measured in any division of this design.

**Evidence.** docs/15-profiles-and-divisions.md:64 (concurrency = 1 GATE), :117 (frees list), :127 (Tuned runs the timing subsample only); docs/05-hardware-and-calibration.md:83-91 (`throughput_score = tasks_passed / wall_clock_hour`, "machine property"), :143 (refuse gate); docs/08-run-protocol.md:33 (units independent and idempotent), :35-46 (serial per-unit sequence); README.md and docs/00-overview.md:39-45 (the two-number claim); docs/adr/0010:Consequences. ~3.8× slot-batching figure from published llama.cpp parallelism benchmarking.

**Resolution.** Rename the metric `throughput_score_serial` or publish it explicitly as a single-stream figure, and state the reason (determinism / replay) on the leaderboard rather than in a gate table. If the aggregate number matters to the thesis, measure it: an unscored, post-suite `rustybench throughput --parallel N` pass over a fixed seed subset costs minutes and yields the batched figure without touching any scored verdict. Correct ADR-0010's consequence list to name concurrency as a throughput lever that Pinned gives up.

**Verifier.** CONFIRMED, and the batching factor holds under a physics check rather than only the citation: decode is dominated by reading the weights once per step, so B concurrently-decoding sequences amortise one weight read across B tokens and aggregate throughput scales roughly with B until compute- or KV-bandwidth-bound — ~3-4x at four slots is the expected result, not an outlier. All the design facts verify: concurrency = 1 is a GATE (15:64) and a 'refuse' with 'Non-negotiable for a ranked row' (05:143); the harness is strictly serial (08:35-46); the workload is 1088 units that 08:33 itself calls 'independent and idempotent'; `throughput_score = tasks_passed / wall_clock_hour` is labelled a 'machine property' (05:83-91); and no document discloses the single-stream framing. Two sub-claims I verified and that strengthen it: 15 §11 is titled 'Honest costs, and what this gives up' and §11.1-11.8 does not mention concurrency, while ADR-0010:43 positively claims 'the throughput half of the two-number thesis survives intact: threads (3.75x), batch/ubatch, flash attention, speculative decoding and all hardware stay free' — and that bullet is wrong twice, because 15 §2.3 sets speculative decoding **OFF for Pinned rows** unless a byte-identity check passes. Third: Tuned frees concurrency (15:124) against a strictly serial client, so the freed knob yields no throughput gain and `tuning_gain` measures ~1.0 for the largest serving lever.

**Verifier correction.** Reframe so it does not read as 'the metric is wrong'. Concurrency = 1 is the *correct* choice given 08:126-132 (8 slots -> 5-8 unique completions for one prompt; vLLM 80 distinct in 1000 at temp 0) — determinism and replay require it. The defect is disclosure, not measurement: an undisclosed single-stream figure, a README line ('what your machine actually delivers per hour') that reads as serving capacity, and an ADR consequence list that claims the opposite. Drop or soften 'the quantity the two-number thesis names is not measured in any division' — for a single-user local runner the serial figure is the operationally relevant one. Lead instead with the two verified ADR-0010:43 errors and the Tuned null-knob.

---

### `R6-S11` — Doc 15's own Tuned example command quarters the Tuned arm's context on llama.cpp, so `tuning_gain` and `capability_delta` would measure the 19.5pp context effect and label it tuning

**MODERATE** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`

**Defect.** §9.3 prints `rustybench run --division tuned --serve <gguf> --tuned-args "-fa on -t 8 -ctk q8_0 --draft-model <gguf> --parallel 4"` with no compensating `-c`. In llama.cpp, `--kv-unified` is "enabled if number of slots is auto"; passing `--parallel 4` explicitly makes the slot count non-auto, so the classic division applies and each slot receives `n_ctx / n_parallel` (ggml-org/llama.cpp #11681). The Pinned twin runs at `-c 32768 --parallel 1` → 32,768 per slot; the Tuned arm at `-c 32768 --parallel 4` → **8,192** per slot. §3.2 states Tuned pins "the per-category context gate", but the gate is probed during the Pinned pass and no re-probe is specified for the Tuned arm.

**Failure scenario.** The two arms differ by 4× in effective context — the single largest measured effect in the entire design space (19.5pp, z = 3.27, p = 0.001, ADR-0010's headline evidence). `verdict_agreement` and `capability_delta` are computed on paired units across those two arms and attributed to "tuning"; `tuning_gain` compares a 32k-context arm against an 8k-context arm on the same machine. §11.4 already flags that `tuning_gain` has a submitter-controlled denominator via threads (3.75×); this is a larger confound in the *numerator*, created by the document's own worked example, and it produces a `capability_delta` with a paired CI that looks statistically clean. If the Tuned arm re-probes context it will instead be gated out of every category (8,192 < any `min_effective_ctx`), so the example command either silently confounds the measurement or cannot run at all.

**Evidence.** docs/15-profiles-and-divisions.md:547-548 (the command), :123-129 (Tuned pins the per-category context gate; measures tuning_gain / verdict_agreement / capability_delta), :661 (§11.4 names only the threads confound); llama.cpp server README `--kv-unified` ("default: enabled if number of slots is auto") and issue #11681 (`--ctx-size` divided by `--parallel`).

**Resolution.** Fix the example to `--parallel 4 -c 131072`, or drop `--parallel` from it. More durably: make the Tuned arm re-run `doctor` probes 2 and 5 and hard-fail if `effective_ctx` differs from the Pinned pass, since Tuned's whole claim is that only the *serving* configuration varied. Add `effective_ctx` to the A/B/A interleave's must-match list alongside machine, weights file and session.

**Verifier.** Mechanism CONFIRMED against primary sources. The llama.cpp server README gives `--kv-unified`: 'use single unified KV buffer shared across all sequences (default: **enabled if number of slots is auto**)' and `-np/--parallel`: 'number of server slots (default: -1, -1 = auto)'. Passing `--parallel 4` explicitly makes the slot count non-auto, so unified KV defaults off and the classic division applies — ggml-org/llama.cpp#11681 is titled 'llama-server `--ctx-size` is divided by `--parallel` and cannot be increased?', and the documented rule is effective_per_slot = n_ctx / n_parallel (e.g. `--ctx-size 32768 --parallel 8` -> 4,096 per request). The document's own Tuned example (15:547-548) passes `--parallel 4` with no compensating context flag, and 15:123 says Tuned pins 'the per-category context gate' while specifying no re-probe for the Tuned arm — so whatever `effective_ctx` that arm actually gets is never checked. Since `verdict_agreement` and `capability_delta` are computed on paired units across the two arms (15:126) and context is the single largest measured effect in the design's own evidence base (19.5pp, z = 3.27, p = 0.001), a context difference between arms is attributed to 'tuning'. §11.4 flags only the threads confound in the denominator; this is a larger one in the numerator, created by the document's own worked example.

**Verifier correction.** One factual fix: the example command carries no `-c` at all, so '32,768 per slot vs 8,192 per slot' is inferred from §9.2's remediation line rather than read off the command. With no `-c`, llama.cpp loads the model's native window (the doc's own geometry block gives n_ctx_train = 262144), and the direction can even reverse. State the mechanism as 'the Tuned arm's per-slot context is n_ctx/4 and nothing in the design pins or re-measures it', not as a specific 4x deficit. The durable resolution is the second one the finding offers — make the Tuned arm re-run doctor probes 2 and 5 and hard-fail on an `effective_ctx` mismatch — and it composes with R6-S10: freeing `--parallel` against a serial client is a pure confound with no throughput upside.

---

### `R6-S12` — §10.1's claim that the row key leaves item pairing intact is false once `categories_scored` is a key component, and no document specifies the paired estimator when two rows scored different category sets

**MODERATE** · *verifier lowered severity* · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/07-statistics.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/adr/0009-paired-core-and-fresh-probe-seeds.md`

**Defect.** §10.1 states: "putting a field in the key does not destroy the *item* pairing — every model in an epoch runs the identical core seed set ([ADR-0009]), so McNemar pairing over items is intact regardless." The code block eight lines above lists `categories_scored` as a key component, and that field exists precisely because two rows do **not** run the same units. The claim and its own key definition contradict each other within one section. 07's reporting rules ("Model comparisons use McNemar on the shared seed set, and state the discordant-pair count") and its cluster bootstrap ("resample task families with replacement, carry all seeds") both presume a single shared item set and give no rule for the asymmetric case.

**Failure scenario.** Model A is submitted from a 32k-context machine (10 categories, 1040 core units — `cross-module`'s 48 dropped); model B from a 128k machine (11 categories, 1088 units). McNemar over "the shared seed set" is undefined: run naively over the union it counts B's 48 `cross-module` units as discordant-against-nothing, biasing the comparison by up to the full weight of a stratum; run over the intersection and the comparison silently answers a different question from either row's published `capability_score`, with no `categories_scored` annotation on the *comparison*. The cluster bootstrap has the same hole — resampling families over the union changes the equal-weight denominator inside the bootstrap loop. The probe detector is affected too: ADR-0009 sizes the sign test at 163 family-level pairs, and dropping `cross-module` plus `perf-optimization` removes 24 of 272 families, shrinking the detector's sample by 8.8% against a floor already stated as "inflation below roughly 4–5 points is undetectable at any probe size we can afford".

**Evidence.** docs/15-profiles-and-divisions.md:594-604 (`categories_scored` in the key, then the pairing claim); docs/07-statistics.md §Reporting rules and §Confidence interval computation; docs/adr/0009 (identical core seed set; 163 family pairs; detector floor).

**Resolution.** State the rule explicitly: paired comparisons and the cluster bootstrap run over `categories_scored(A) ∩ categories_scored(B)`, the intersection is published with the comparison, and a comparison whose intersection omits a core category is refused. Recompute and publish the probe detector's sensitivity floor as a function of the number of families actually planned, not the nominal 272.

**Verifier.** The textual contradiction is REAL and novel: 15:604 asserts 'putting a field in the key does not destroy the *item* pairing — every model in an epoch runs the identical core seed set ([ADR-0009]), so McNemar pairing over items is intact regardless', eight lines below a key (15:597) containing `categories_scored`, a field that exists precisely because two rows do not run the same units. But the blast radius is well below moderate and three of the four consequences do not hold. (i) 07:294 already says 'McNemar on the **shared** seed set', which reads naturally as the intersection; the union reading the failure scenario requires is a misreading, not the specification. (ii) The 'comparison whose intersection omits a core category' case cannot arise: 15:99 refuses the *run* when a core category is unattemptable, so every published ranked row carries all five core categories and every intersection contains core5 — which also independently rescues 04:153's 'always share a denominator'. (iii) The detector arithmetic is right (24 of 272 families = 8.82%) but lands on a detector the design already declares broken twice over (R4-S1's suppression attack, R5's `probe-reroll` at 0% detection), so re-sizing it changes nothing actionable. What genuinely remains is one missing sentence: the cluster bootstrap's family set (07:266-278, 'resample task families with replacement') is unspecified when two rows scored different category sets, and resampling over the union would move the equal-weight denominator inside the bootstrap loop.

**Verifier correction.** Report as minor and retitle to the residual: 'the cluster bootstrap and the paired comparison do not specify their family set when two rows scored different categories, and §10.1 asserts an item-pairing invariant its own key definition breaks.' Drop the McNemar-is-undefined framing, drop the core-category-intersection case (unreachable), and drop or footnote the 8.8% detector limb. Keep only the first sentence of the resolution.

---

### `icc-sensitivity-grid-tabulates-superseded-estimator` — The ICC sensitivity grid can no longer answer the question it exists to answer, because it tabulates only the pooled overall CI that round 5 replaced

**MINOR** · surface: `bench-stats` · docs: `docs/07-statistics.md`

**Defect.** docs/07-statistics.md:206-216 is the document's only tool for "what if ICC is not 0.3". Its overall-CI column is headed "+/-CI overall (pooled -- see above)" and gives +/-3.8%/4.1%/4.7%/5.2% at ICC 0.2/0.3/0.5/0.7. The ICC 0.3 entry, +/-4.1%, is exactly the figure round 5's `overall-ci-wrong-estimator` replaced with +/-4.9%. The label is honest, but there is no equal-weight column, so the grid cannot express the sensitivity of the number the leaderboard actually sorts on.

**Failure scenario.** A reader wanting to know how the headline moves if ICC is 0.5 rather than 0.3 reads the grid, gets +/-4.7%, and has no way to know that the corresponding equal-weight figure is different -- while the suite table 55 lines earlier gives +/-4.9% at ICC 0.3, higher than the grid's worst case of +/-5.2% at ICC 0.7. The two tables are not on the same scale and a reader comparing them draws the wrong conclusion about how much ICC matters.

**Evidence.** /private/tmp/claude-501/-Users-iwan/4591b98d-7b92-439f-ae45-1a2625feee97/scratchpad/recheck.py section 1; grid values reproduce exactly from the pooled formula.

**Resolution.** Add an equal-weight overall column to the sensitivity grid computed with the same Var = (1/K^2)*Sum_c Var_c estimator as the suite table, and keep the pooled column labelled as the unit-level figure. If the grid becomes two-dimensional over (rho_f, rho_s) per the design-effect finding, fold this in at the same time.

**Verifier.** The verifiable core holds. I reproduced the grid exactly from the pooled formula (ICC 0.2/0.3/0.5/0.7 -> effN 680/573/435/351 -> +/-3.8/4.1/4.7/5.2%), and there is genuinely no equal-weight column, so after R5 replaced the headline estimator the document's only sensitivity tool no longer covers the number the leaderboard sorts on. That is the same fix-creates-a-gap pattern and it is concrete. Minor is honest, and the document does label the column '(pooled - see above)', so this is a completeness gap rather than a wrong number. It survives only just: it is close to a documentation note.

**Verifier correction.** The failure scenario contains a false comparison and must be rewritten. '+/-4.9% at ICC 0.3, higher than the grid's worst case of +/-5.2% at ICC 0.7' is arithmetically backwards - 4.9 is lower than 5.2. The correct statement: the suite table's equal-weight +/-4.9% at ICC 0.3 sits between the grid's pooled ICC-0.5 entry (+/-4.7%) and its ICC-0.7 entry (+/-5.2%), so a reader cross-reading the two tables will conclude the headline is already at its worst case when in fact the grid is measuring a different estimator entirely.

---

### `paired-bootstrap-named-for-unpairable-comparison` — "Category comparisons within a model use the paired bootstrap" names a paired estimator for a comparison with no pairing structure

**MINOR** · surface: `bench-stats` · docs: `docs/07-statistics.md`, `docs/04-categories.md`

**Defect.** docs/07-statistics.md:293. Two categories share no families, no shapes and no seeds -- the corpus partitions families across categories by construction (04-categories.md). The sampling variability of two category means is therefore independent, and there is nothing to pair. The intended procedure (resample, recompute the difference, which is strictly better than overlapping-CI eyeballing) is correct; the name is not.

**Failure scenario.** An implementer building `bench-stats` from this line looks for the pairing key that makes the comparison paired, does not find one, and either invents a spurious alignment (pairing category A's family i with category B's family i, which is meaningless and produces an arbitrary covariance term) or pairs on the resample index in a way that silently correlates two independent strata. Both produce a wrong variance with no symptom. The same sentence is the one that licenses the 55 uncorrected comparisons in `no-multiplicity-correction-anywhere`, so it is worth rewriting regardless.

**Evidence.** docs/07-statistics.md:293 read against docs/04-categories.md's per-category family partition; no shared cluster exists between any two categories.

**Resolution.** Reword to: "Category comparisons within a model bootstrap the difference of the two category means directly -- resampling each category's shapes independently -- rather than eyeballing overlapping CIs", and attach the multiplicity rule to the same bullet.

**Verifier.** CONFIRMED on the facts. 04-categories.md partitions 272 families across the eleven categories with no sharing (5 core x 40 + 6 probe x 12), so two category means within one model have no common family, no common shape and no common seed, and their sampling variability carries no covariance term. There is nothing to pair, and 'the paired bootstrap' at 07:293 is a misnomer for what is really 'bootstrap the difference of two independent strata'. The intended procedure is correct; only the name is wrong. Minor is honest.

**Verifier correction.** Soften the failure scenario. The specific hazard described - an implementer pairing category A's family i with category B's family i - is not credible when the two categories have 40 and 12 families and the alignment would be undefined for 28 of them. The defensible cost is the naming plus one real consequence: this is the single sentence that licenses the uncorrected within-model comparisons, so the multiplicity rule from no-multiplicity-correction-anywhere should attach to this bullet when it is rewritten, and the rewrite should say explicitly that the comparison is licensed only among the five core categories, since 07:243-244 already bars orderings on probe categories.

---

### `R6-S13` — `budget_tokens` is 32768 / conditional-32768 / 16000 across three documents, and the manifest classifies it as a submitter-settable knob while doc 15 pins it

**MINOR** · surface: `fix-created-holes` · docs: `/Users/iwan/Desktop/Rustybenchmark/docs/02-task-format.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/12-schemas.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/15-profiles-and-divisions.md`, `/Users/iwan/Desktop/Rustybenchmark/docs/08-run-protocol.md`

**Defect.** Three values and two ownership models. 02:70 sets `budget_tokens = 32768` **unconditionally**. Doc 15 §2.2 and §11.9 specify "≥ 32768 **for reasoning-detected rows**" — conditional, with non-reasoning rows presumably staying at 16000. 12:218's on-wire manifest still carries `submitter_config.budget_tokens: 16000`. Separately, the field lives in `submitter_config` — the block 08 defines as "any submitter-settable knob that does exist goes into the signed manifest and is displayed" — while 15 §3.1 lists `max_tokens` among the Pinned division's pins and §4 emits it in the harness-authored `[request]` block with `tier = "harness_authored"`. Doc 15's own reference profile compounds it: `[reasoning] mode = "off"` with `budget_tokens_applied = 32768`, and doctor prints "reasoning off / max_tokens 32768" — the conditional rule applied unconditionally in its own worked example.

**Failure scenario.** An implementer reading 12 sends `max_tokens: 16000`; one reading 02 sends 32768; one reading 15 sends 32768 only when reasoning is detected. All three are Pinned rows with the same `sampling_profile_id` and the same `profile_hash` scheme, and the row key cannot distinguish them because `max_tokens` is inside the hashed `[request]` block in one design and inside `submitter_config` in another. Because the field is nominally submitter-settable, a submitter can also lower it deliberately — which is a direct lever on `budget_exhausted_rate`, the field doc 15 §5.4 introduces as a *taint* threshold, so the submitter controls both the numerator behaviour and the knob the taint is measured against.

**Evidence.** docs/02-task-format.md:70-75; docs/12-schemas.md:218; docs/15-profiles-and-divisions.md:61, :113, :201-206, :256-258, :357, :504, :682; docs/08-run-protocol.md:101.

**Resolution.** Pick one: make `budget_tokens` harness-authored and a function of detected `reasoning_mode`, move it out of `submitter_config` into the hashed `[request]` block, and delete it from 12's manifest `submitter_config`. Update 02 to state the conditional rule rather than the unconditional 32768, and fix doc 15's reference profile so `mode = "off"` carries the non-reasoning budget.

**Verifier.** CONFIRMED, every citation checked verbatim. Three values: 02:70 `budget_tokens = 32768` with no condition attached (the comment justifies it from the reasoning measurement but the value is unconditional); 15:61 pins '`max_tokens` >= 32768 **for reasoning-detected rows**' and 15:682 restates the conditional; 12:218's on-wire manifest still carries `submitter_config: {..., budget_tokens: 16000, ...}`. Two ownership models: the field sits in `submitter_config`, which 08:101 defines as 'any submitter-settable knob' and 12:232 says is 'signed and **displayed on the leaderboard**. Every knob a submitter can turn must be visible', while 15:113 lists `max_tokens` among the Pinned division's pins and 15:256-258 emits it inside the harness-authored `[request]` block with `tier = "harness_authored"`. And the reference profile applies the conditional rule unconditionally to itself: 15:202 `[reasoning] mode = "off"` alongside 15:206 `budget_tokens_applied = 32768` and 15:504 doctor printing 'reasoning off / max_tokens 32768'. Minor is the right severity — it is value-and-ownership drift across four documents, all textual, all cheap to fix, and it is novel (02's 32768 and doc 15 are both post-round-5).

**Verifier correction.** Drop the exploit limb, which inverts. Lowering `budget_tokens` *raises* `budget_exhausted_rate`, which per 15 §5.4 taints the run — so it is not a lever an attacker wants to pull, and 'the submitter controls both the numerator behaviour and the knob the taint is measured against' overstates a knob whose only free direction is upward and honest. The finding is about drift and ownership, not gaming. The resolution (harness-authored, a function of detected `reasoning_mode`, moved into the hashed `[request]` block, deleted from `submitter_config`) is correct as written and also closes the row-key ambiguity.

---

## The 46 unassessed findings

Raised by agents whose verifiers died. **Unverified — treat as leads, not findings.** Full text is in
the run transcript (`wf_da87dde8-635`). Titles only, so the record is not lost:

- **`cleanup-arithmetic` (13):** re-derivations of the round-5 statistics corrections — the stratified
  CI, the `standard` row recomputation, the `deep` @60 tok/s decomposition, the corpus split.
- **`cross-doc` (19):** a fresh contradiction sweep over 26 documents after the round-5 and doc-15
  patches.
- **`unattacked-surfaces` (14):** the agentic track, epoch rotation, the contributor workflow, and
  leaderboard aggregation — none of which had ever been examined.

Two of these three overlap heavily with verified findings above (`R6-S7` and
`standard-suite-overall-ci-self-contradiction` were both found independently by `fix-created-holes`
and `bench-stats`, which is corroboration across surfaces). The `unattacked-surfaces` block has no
such overlap and is the one genuinely unexamined area remaining.

## The meta-question, answered honestly

Six rounds have produced 6, 16, 15, 6, 58 and 72 findings. **The design is not converging, and the
distribution says why.** Round 6's verified findings concentrate almost entirely in material written
in the previous two commits: 10 of 23 are in `fix-created-holes`, i.e. defects *created by round 5's
own repairs*, and 11 of 23 are in `bench-stats` — machinery that six rounds never examined because it
was never written down beyond a crate name.

That is churn, not settling. Two structural conclusions:

1. **Review has passed the point of diminishing returns, and round 5's conclusion stands
   strengthened.** Almost every remaining uncertainty is a *measurement* — ρ, ICC, the shape
   component, mining yield, backend spread — and measurements need code. Continuing to review
   documents that describe unbuilt machinery mostly generates more documents.
2. **The fix-creates-the-next-hole pattern is now four for four and needs a mechanical answer, not
   more diligence.** Every instance has been a local edit to a globally-quantified invariant. The
   answer is to make the invariants executable: a script that recomputes every published table from
   its stated formulae and fails CI on a hand-edited cell would have caught R1-S1, R5's arithmetic
   block, and R6-S1/R6-S7 — four findings across three rounds, one script.

## Round 7 scope

**Do not run round 7 as a document review.** The three things worth doing, in order:

1. **Build the invariant checker.** One script, four historical findings.
2. **Write down the statistical machinery** — pass predicate, core-seed collapse rule, ICC estimator,
   bootstrap unit, multiplicity policy — because `bench-stats` findings show it is currently
   undefined, and undefined machinery cannot be reviewed, only guessed at.
3. **Verify the 46 unassessed findings**, or accept them as lost and move on. The
   `unattacked-surfaces` block is the only one with no corroboration from elsewhere.

**Prediction of which round-6 fix creates the next hole:** whichever change specifies the pass
predicate on `task_score`. It is referenced by `throughput_score`, `time_to_first_pass`, McNemar, the
sign-test detector, the probe discordance calibration and `budget_exhausted_rate` — six consumers,
each of which currently assumes its own implicit threshold. Defining it once will silently change all
six, and at least one of them will have been depending on the old implicit behaviour.
